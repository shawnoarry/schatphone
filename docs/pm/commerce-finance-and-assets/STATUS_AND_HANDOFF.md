# Commerce Finance And Assets Status And Handoff

Updated: 2026-08-06

This file is the handoff page for Shopping, logistics, Food Delivery, Wallet, Assets, and Stock work.

## 1. Current Status

Status: `PARTIAL_DONE`

What is already landed:

1. Shopping and Food Delivery both have solid order-lifecycle baselines;
2. logistics and delivery status concepts exist and can connect into Chat/service-account style messaging;
3. Shopping and Food Delivery can feed Wallet as downstream records;
4. gift and shared-meal relationship-memory flows are now wired through Wallet recording, with Wallet support facts treated as supporting-only lineage inside the upstream order memory;
5. delivery route context can be shown as read-only context without moving order ownership into Map.
6. Shopping checkout, Shopping logistics events, Food Delivery checkout, and Food Delivery order events can push service-account notification messages into existing Chat Directory service accounts.
7. The active World Pack can now provide a Shopping marketplace context for `survival_city`: WorldBook opens Shopping as `补给站`, and Shopping offers a safe Daily Fresh / Grocery filter without creating or mutating commerce records.
8. The active World Pack can expose Food Delivery dispatch entries for `survival_city` and confirmed nonstandard `dispatch_board` entries, but an app binding is now launch context only unless it carries an explicit `uiThemePackage.enabled=true`. Without a Food Delivery UI theme package, Food Delivery uses its original UI and defaults while keeping restaurants, menus, carts, food orders, and delivery events owned by Food Delivery. World Pack boundary explanations are documented and tested, but they should not render as an in-app explainer card.
9. Food Delivery now has a platform/store split: the Food entry opens a platform browser, the pseudo-folder opens peer shop mini-app surfaces, and cart/order/event/Wallet/Map/Chat ownership remains centralized in Food Delivery and the related source modules.
10. Food Delivery platform mode now opens as a pseudo-folder style surface: a fixed Food platform entry plus shop-app restaurant entries. Category keys continue to filter shops and do not own order/cart behavior.
11. Moon Bistro now has the first Food Delivery shop template treatment, `dark_tray_menu`, while checkout and order ownership remain in Food Delivery.
12. App Store can now surface Food Delivery restaurants as `food_delivery`-bound folder mini apps, save App Store-side display/icon/cover/short-description/tag/template facade presentation for those entries, show their target-folder ownership boundary, and add/remove them from the target folder without deleting source records. Food Delivery reads presentation and install-state fields for launcher/store display. App Store also surfaces Shopping platform services as `shopping`-bound folder mini apps, opens Shopping with service/shop-entry context, lets Shopping render the same App Store-side cover facade, and lets Shopping hide uninstalled service mini apps from its folder list. `Add mini app` in App Store is a target-folder handoff: Food Delivery receives a restaurant-creation context and Shopping receives a Shopping-owned creation workspace context. This is install-entry presentation, target-folder placement, and owner handoff only: App Store does not own restaurant records, menus, products, carts, checkout, orders, shop favorites/recent lists, consumer category filters, Wallet/Assets handoffs, Map/Calendar/logistics handoffs, or Chat service pushes.
13. Food Delivery menu items now have a focused item-detail sheet: tapping a dish opens description, base ingredients, image source, and add-to-cart; a small edit icon updates only that item copy/image through Food Delivery-owned menu records.
14. Moon Bistro now opens as a shop-first surface: the Food platform hero/list chrome is hidden, the store owns the first screen, cart becomes the ordering anchor, and Map/order/Wallet support panels are folded behind Order & delivery.
15. Moon Bistro checkout is now shop-local: checkout opens a confirmation sheet before creating an order, shop orders/events/Wallet suggestions are scoped to the opened shop, and Food Platform no longer renders shop cart/order/Wallet/Map support panels as a total controller.
16. Food Platform now has a consumer-facing discovery homepage instead of the earlier backend-like platform card stack. It shows a brand/address header, project-owned rider asset near the search field, a shorter horizontal ad-carousel rail, ten icon-first category shortcuts in a stable two-row grid, a horizontal platform-merchant rail, and a light bottom navigation row. The restored category density follows the useful part of the 2026-06-08 layout while replacing its duplicate Chicken/Pizza routing with distinct platform filters and adding meaningful Noodles/Sushi filters. Category source sheets remain reference-only because their checkerboard is baked into RGB PNGs; ten exact transparent-PNG requirements are exposed through `data-required-asset` and documented for later asset rounds. Follow-up layout and interaction passes keep the existing colors, banners, rider, and healthy merchant imagery while adding a delivery-address chooser, a lightweight greeting, richer merchant rating/delivery/ETA summaries, a real expand/collapse path from the merchant rail into a full list, hidden native rail scrollbars, focused notification/cart panels, functional platform favorites, and route-driven campaign pages for all three homepage banners. The collapsed `All` rail now shows three shops randomly selected once per mounted platform session, remains stable during that session, and leaves the complete eleven-shop set to `View all`; category filters continue to show complete matching results. The three campaigns now have different product jobs and layouts: membership claims/uses a weekly delivery benefit, Weekend Picks uses a large-concept poster plus three listed rewards and a one-draw App-local reveal, and the lunch editorial recommends specific dishes. The weekend result is presentation-only and creates no Wallet, Assets, Chat, Map, or order effect. Mine is a route-driven platform account page with order/saved summaries, selectable delivery addresses, membership, past-order, delivery-contact, and update entries; delivery contact remains anchored to platform-owned orders and does not create Chat behavior. Search and Saved open distinct route-driven platform pages instead of reusing the homepage composition; platform search filters internal merchants by shop, cuisine, category, badge, and menu text. All eleven merchants now open through one route-driven full-page merchant template rather than a focused sheet. That page has a wide Hero, rating/delivery/minimum-order summary, `112x112` menu photography, add/decrease/increase controls, and a fixed cart entry; it hides the platform bottom navigation and returns to Home, Search, Saved, or Campaign while preserving `from/homePage`. Four selected merchants use distinct Chinese advertising covers on discovery surfaces while their transparent marks remain compact order identities; the others keep food photography or food-photo requirements. The cart continues into a route-driven confirmation page, App-local payment choice, submitted-order detail/success page, and platform-only order list backed by persisted `platformOrders`. This flow does not call the independent-shop checkout action and does not create Chat, Wallet, or Assets effects. Platform cart/order state stays separate from restaurant `cartItems`/`orders` and never merges same-level independent shop apps such as Moon Bistro. The platform pulls homepage banners, merchant photography/advertisements, compact marks, and rider decoration from `public/images/ui-assets/apps/food-delivery/platform/` rather than remote placeholder images. The shop creation/menu editor is hidden from ordinary platform browsing and appears only for the App Store create-shop handoff.
17. Moon Bistro's first visual asset pass is now connected to the project UI asset library: fresh seed data uses the local Moon Bistro cover and dish photos, the dark shop header uses that cover as an immersive background, dish cards keep the circular embedded-photo treatment, and the one-dish edit sheet still lets users replace copy and images through URL/Gallery sources.
18. Moon Bistro now has a fuller browseable shop menu: fresh or migrated saves receive nine seeded dishes across warm soup, rice set, grill, seafood, greens, pasta, and dessert sections without overwriting user-edited dish copy/images. The dark shop surface adds a sticky side section rail, filters the right-side dish list by section, keeps circular embedded dish photos, hides the empty cart until the user adds food, and keeps order/Wallet/Map support hidden until there is real order support content.
19. Wallet has a persisted primary-currency setting, and Food Delivery watches that setting. Current restaurant/menu source values remain unchanged while menus, modifiers, delivery fees, Baemin campaign values and structured minimum orders, carts, and checkout totals numerically quote through Wallet across every current facade. New Food Delivery orders freeze source and quoted money provenance; existing orders and Wallet transactions keep the currency and amount they were created with and remain historical snapshots.
20. Wallet owns the shared currency registry and editable reference exchange-rate table. The default financial coordinate is USD/CNY, system currencies are available by default, and WorldBook's optional capability Packs can inject custom world currencies into Wallet. Wallet keeps the primary-currency choice and effective rates; World Pack stores only world-specific declarations. Chat transfer cards and sourced Chat ledger records use Wallet currency options instead of a hard-coded CNY field. The promoted foundation and current commerce adoption from `WALLET_CURRENCY_AND_MONEY_CONVERSION_PLAN.md` are implemented: currency exponents, integer-minor-unit quotes, deterministic half-up rounding, locale-aware formatting, explicit missing-rate results, decimal-string rate revisions, legacy numeric-rate migration, Shopping/Food Delivery current-value quotation, and immutable new order/Wallet quote snapshots now live behind Wallet. Mixed-currency Shopping settlement, future refund snapshots, and the general poster-anchor schema remain unimplemented.
21. Shopping can now be represented in Chat through source-owned `share_card` objects. The active Chat `+` Shopping send path converts ordinary products into `product_link` cards and reserves direct gift wording for gift-card / voucher / virtual-gift-like products. This removes the earlier implication that every product card is an instant user-owned gift while keeping Shopping as the owner of products, checkout, orders, Wallet handoff, Assets suggestions, and later gift/redeem state.
22. Peach Cloud is now the second built-in independent Food Delivery shop app and the first dedicated `dessert_window` implementation. Exact Figma home node `47:23` is adapted into a pink-mist header, a restored `3:2` horizontal brand Hero, five always-visible SVG category shortcuts, a switchable poster/menu content region, and a lightweight Pink Mist fixed navigation with Petal Rouge selected states rather than another shared shop-home template. Home deliberately removes the previous Best Seller and Recommend menu duplication: its region below the category navigation defaults to one complete `2:3` advertisement at a time and replaces that carousel with the selected category's menu; `返回海报 / Posters` restores the campaign view. Search remains a separate point-of-sale route, while Home and Discover/New show one complete `2:3` advertisement at a time with all poster copy integrated into the accepted image. Detached header arrows are removed; each poster owns left/right edge hit zones for previous/next navigation. White Peach Lime and Waxberry Lychee open separate long campaign routes with a portrait Hero, two landscape story bands, localized copy, Wallet-following price, and an explicit quantity CTA into the standard detail sheet. Peach Club remains separate, and the mascot poster still opens Merch; Merch keeps its existing image and navigation while replacing the old left solid-color mask with direct editorial copy. The original-size Golden Peach horizontal advertisement remains one fixed `48 CNY` product and opens the standard product detail sheet; no Pairing route or Solo/Double/Party chooser remains. Bag, Orders, and order detail stay Peach Cloud-local pages; mixed food/merchandise Bag leads into the existing checkout confirmation, and submitted orders open their own progress detail. Its current brand palette is Iron Grey `#444545`, Jet Black `#2B303A`, pale green `#F2FBE0`, Petal Rouge `#FD6C93`, and Pink Mist `#FDA1B8`; the strong pink always pairs with dark text instead of small white text. Seventeen brand-voiced menu items and three cash-purchase mascot goods use Food Delivery-owned detail, quantity, checkout, backup, order, and event runtime. The formal asset pack contains 27 PNGs, 6 WebPs, and 6 SVGs on stable diagnostic paths, while five original inactive category SVGs remain available for other shops; old saves update legacy built-in copy only when the old seed title is unchanged.
23. Dash Grill is now the third built-in independent shop app and the first `quick_service_chain` implementation. Its original red, mustard, paper, and ink UI has a promotion-led Home plus real route-driven Menu, Deals, Bag, Orders, and order-detail pages. Home popular items use compact counter tickets and Menu uses full-width image-led order tickets with stable item numbers, section identity, a perforated price/add footer, first-real-section defaulting, and cross-section search. Category and popular-item rails expose a thin branded scrollbar and keyboard focus instead of hiding overflow. Product detail now becomes an unbranded tray/order ticket instead of the generic minimal sheet. `Dash Double Stack Combo` and `Golden Chicken Stack Combo` keep their main fixed and require one side plus one drink; Loaded Cheese Fries and Vanilla Cloud Shake expose real price deltas. The builder exposes `2/2` completion, live choice summaries, existing side/shake photography, branded fallbacks for pending drink media, and a compact quantity/total action bar. Crisp Chicken Tenders now requires one of three dipping sauces instead of promising an unimplemented choice. Quick-add opens required configuration, distinct selections receive distinct line IDs, and localized combo/sauce selections plus selected unit prices persist through restaurant-scoped Bag, checkout, backup, and order snapshots. All ten built-in items now follow system language with `zh-CN` as the default and accurate `en-US` retained; search indexes both built-in languages plus current saved copy, while same-ID user-authored fields remain literal. Exact old seed copy migrates to complete-combo wording without touching same-ID user edits. The delivered one cover and ten product PNGs remain unchanged on stable `dash-grill/` paths; item `01` and `02` currently act as honest main-item imagery, while complete default tray photographs for `dash-grill/combos/dash-grill-double-stack-combo-01.png` and `dash-grill/combos/dash-grill-golden-chicken-combo-01.png` are `NOT_GENERATED / NOT_CONNECTED`. Focused store/view plus desktop Chromium and simulated Pixel 5 route coverage passes, while user visual acceptance of the ticket density, detail composition, and crops remains pending. Moon Bistro's built-in cuisine and unchanged legacy menu copy now migrate from late-night positioning to modern fine dining while preserving user-authored text.
24. Jade Hearth is now the fourth built-in independent shop app and the first `jade_table_menu` implementation. Its original rice-paper, ink-green, and cinnabar UI uses a full-width editorial table-book home, shared/solo meal selector, menu-book categories, feast collections, a focused bag, shop-scoped orders, and a dedicated order-detail page instead of reusing the previous shop layouts. Twelve dishes span house table, small plates, wok favorites, claypot, rice/noodles, and tea/sweets while sharing the Food Delivery-owned restaurant-scoped cart, checkout, and order runtime. Chat order links open the dedicated order detail; complete shop history, persisted delivery events, explicit status actions, and the existing Wallet-record adapter remain available inside that branded page. Fresh and existing saves receive only missing stable seed IDs, same-ID user edits remain intact, built-in seeds retain protected capacity beyond the 360 user-menu limit, and explicit backup restore remains snapshot-faithful. Its one cover and twelve product PNGs are delivered on the stable `jade-hearth/` paths, with first-round/retry decisions and editable masters retained under `output/imagegen/jade-hearth/`; focused desktop Chromium and simulated Pixel 5 Home/Menu/Detail review passed after the built-in whole-fish Home card alone switched to `object-contain`, leaving the source PNG and Menu/detail crops unchanged.
25. Verdant Day is now the fifth built-in independent shop app and the first `minimal_light_food` implementation. Its grey-white canvas, full-width mascot-led English brand Hero, circular food photography, icon-led Home category shortcuts, focused Menu tabs, spacious product rows, dedicated product detail, light fixed navigation, branded bag, checkout, order list, and order detail form a route-driven app instead of appending generic shop panels below Home. Its semantic palette assigns Leaf to primary actions and selected progress, Coral to promotional emphasis, Gold to restrained highlights and warnings, and calm neutral surfaces to ordinary controls. Home category controls are destination shortcuts without a false persistent selection; Menu removes the overlapping `All` filter, defaults to the first real non-empty category, keeps global search grouped by category, and conditionally exposes unknown user-authored sections as `More`. Twelve items span salads, warm bowls, wraps/toasts, drinks, and light sweets while sharing Food Delivery-owned menu, restaurant-scoped cart, checkout, order, and event state. Fresh and existing saves receive only missing stable IDs without overwriting same-ID edits; explicit backup restore remains snapshot-faithful. The active English Hero and its text-free edit master are delivered under `verdant-day/brand/`; two photography concepts under `verdant-day/promotions/` now support the active campaign and popup. The complete `01` through `12` product pack is delivered as `768x768` full-square photography with center-safe compositions, while the shared diagnostic placeholder remains available for load failures. Exact Figma node `1:73` context was requested first, but the Starter plan returned `You've reached the Figma MCP tool call limit on the Starter plan.`; this implementation therefore adapts the user-approved visible canvas direction and is not claimed as an exact-node reproduction.
26. Peach Cloud's refreshed shop presentation keeps Food Delivery ownership unchanged while making Home, Search, Discover/New, Campaign, Club, Merch, Bag, Orders, and order detail visually and structurally distinct. The 17 stable built-in menu IDs still resolve to 5 fresh-fruit specials, 4 frozen desserts, 3 tea/coffee drinks, 2 baked sweets, and 3 seasonal products, with Peach Cold Brew Tonic kept outside the fruit-drink category. Migration updates known historical seed titles and menu sections, adds only missing new stable IDs, and preserves user-authored same-ID titles, copy, prices, ingredients, and custom images. Home renders no menu product cards while its category shortcuts and Search keep the full point-of-sale path. Three accepted `1024x1536` posters first expanded the formal pack from 30 to 33 files and provide image-embedded launch, seasonal, and mascot-merchandise advertising without partial adjacent slides. Detached header arrows are now removed in favor of poster-edge previous/next zones. White Peach Lime and Waxberry Lychee product posters open dedicated long campaign pages instead of the quantity sheet; six accepted text-free WebPs add two product-specific one-Hero/two-story-image sets and expand the formal pack from 33 to 39 files. Each page code-renders localized editorial copy and a Wallet-following price, then opens the existing product detail through its final quantity CTA. The mascot poster still opens Merch, whose campaign image and route remain unchanged while its Hero copy no longer sits on a left solid-color mask. The White Peach Lime poster additionally has one versioned experimental derivative whose baked `26 CNY` field is removed and replaced on Home plus Discover/New by a poster-specific percentage slot driven by Wallet currency and exchange rates. Its original poster remains unchanged; the Waxberry Lychee and mascot posters retain baked prices; the derivative is excluded from the formal 39-file count. CNY `26`, EUR `3.34`, and KRW `5015` formatting passed focused component coverage. Desktop Chromium, simulated Mobile Chrome, and manual `393x851` review passed poster-edge routing, both campaign media sets, complete subject framing, CTA flow, Merch Hero treatment, console state, and horizontal overflow. The original Golden Peach card retains its horizontal proportion, single `48 CNY` price, and click-to-detail behavior. Editable masters, exact prompts, reference roles, candidates, and acceptance evidence remain under `output/imagegen/peach-cloud-refresh/`, `output/imagegen/peach-cloud-dynamic-price-pilot/`, and `output/imagegen/peach-cloud-campaign-pages/`, while runtime uses only accepted files under `public/images/ui-assets/apps/food-delivery/peach-cloud/`. Route-driven merchandise remains ordinary cash purchase inside the restaurant-scoped Food Delivery bag; no membership points or separate reward persistence is added. This campaign round and the one-poster price experiment do not claim a shared finance-wide poster schema, migration of the other posters, or conversion of historical records.
27. River Noodles, Daylight Cafe, and Sugar Lane now each seed nine complete menu items across four shop-specific sections. Fresh saves use stable project-local contracts for one Hero and nine product PNGs per shop; existing saves receive missing restaurant/menu IDs, migrate only the six exact historical default Unsplash image URLs plus unchanged legacy copy for the three original menu items, preserve all other same-ID user edits and custom URLs, retain built-in capacity beyond the 360 user-item limit, and keep explicit backup restore snapshot-faithful. `FOOD_DELIVERY_IMAGE2_ASSET_PROMPTS.md` records all 30 paths plus bilingual food semantics and three different photography capsules. This was the shared menu-and-contract milestone; image delivery and visual acceptance are tracked per shop.
28. Daylight Cafe's one cover and nine product PNGs are now delivered on the stable `daylight-cafe/` runtime paths. The accepted first-round candidates preserve the shop's bright morning window light, light terrazzo, Cream/Sun/Sky/Leaf/Espresso color roles, distinct coffee/brunch/bakery semantics, and unique compositions; editable masters, full/pilot/retry request files, contact sheets, and acceptance decisions remain under `output/imagegen/daylight-cafe/`. Desktop Chromium and `393x851` mobile review confirms complete local image loads, recognizable thumbnails, no horizontal overflow, and no console warning/error. Its journal cover crop is focused at `68%` to retain the center-right breakfast group, while Daylight Cafe detail images alone use `object-contain` on Cream so tall glasses, cup handles, plate rims, pastries, and toast are not destroyed by the shared shallow detail slot. River Noodles and Sugar Lane still retain `20` ungenerated formal PNGs.
29. Harbor Roast is now the sixth built-in independent shop app and owns the non-general `harbor_roast_chain` facade. Its `#C67C4E`, `#EDD6C8`, `#313131`, `#E3E3E3`, and `#F9F2ED` palette supports a large three-slide campaign Home, separate member/new-product/Roast Passport pages, an original Captain Roast cup-and-anchor IP, focused Menu and item detail, a branded mixed bag and confirmation sheet, shop-scoped order history, and dedicated order detail. The Captain Roast commercial band and Passport lead to a route-driven Supply station with four product advertisement details. Food Delivery owns the persistent bean-stamp balance and stable merchandise catalog; eligible redemptions deduct each product's distinct stamp cost and add a zero-price `redeemed_gift` line, while cash merchandise adds a priced `purchase` line. Insufficient balances stay disabled, removed unsubmitted gifts refund their stamps, and mixed drink/purchase/gift checkout preserves acquisition snapshots. Browsing, redemption, purchase, and bag actions add no Wallet, Assets, Chat, Map, or external reward records; submitted orders retain the package's existing service-account notification path. Ordering records `delivery` or `pickup`; pickup further records `takeout` or `dine_in`, removes the delivery fee, and remains backward-compatible because historical orders normalize to delivery. The existing twelve stable menu IDs, same-ID edit preservation, and App Store identity entry remain unchanged. The expanded `34`-PNG formal contract is fully delivered and connected: three brand/App Store assets, six campaign assets, twelve drink/bake product photos, five Supply/merchandise assets, six order-state illustrations, and two empty-state illustrations. Desktop Chromium and `393x851` simulated mobile media review passed across Home, campaigns, Supply/detail, mixed Bag, Orders, order detail, and App Store defaults; named physical-device proof remains separate.
30. Independent shop bags are now persistent and restaurant-scoped. Standard shops, Peach Cloud, Dash Grill, Jade Hearth, Verdant Day, and Harbor Roast may all retain cart lines at the same time, while each facade displays, updates, and checks out only its own lines. Adding in one shop never clears or names another shop's bag, no replace-or-cancel dialog remains, and checkout removes only the submitted restaurant's lines. Platform cart/order state stays separate and unchanged; the persisted `cartItems` backup shape remains compatible.
31. Food Delivery offers six general browsing structures: `standard`, `cafe_counter`, `convenience_shelf`, `street_food_stall`, `daypart_journal`, and the restaurant-agnostic `menu_mosaic` category atlas. Each reusable structure opens the first real category, omits `All`, reuses Food Delivery item detail/add/cart/checkout/order ownership, accepts App Store identity and cover overrides, and renders template-native browse and item-detail fallbacks while formal media is missing. The six brand-owned facades remain visible only for the shop currently using them. Focused Vitest plus desktop Chromium and simulated Pixel 5 Playwright cover template identity, category switching, non-built-in overrides, image/fallback loading, detail fallback, and horizontal overflow. River Noodles and Sugar Lane still need their `20` formal PNGs; Harbor Roast's expanded `41`-PNG pack is delivered, while named physical-device proof remains separate.
32. Food Platform now has a multi-merchant photography freeze in `FOOD_DELIVERY_IMAGE2_ASSET_PROMPTS.md` and renders its consumer name as `Baemin`. Its original full-bleed mint entry icon uses a delivery bag, route pin, heart, and motion mark without generating or approximating an official Baemin logo, wordmark, or mascot. Baemin-owned platform campaigns, categories, order states, and decorations may share mint-teal platform styling, but the eleven internal merchants keep separate photographic identities. Food remains photographic across all merchant assets; the scene and production language ranges from pure-color editorial sets and real-food-plus-original-IP compositions to intentionally ordinary stall/mobile-camera images with controlled mixed light, common utensils, and imperfect framing. The `55` menu paths remain eleven separate five-image shop families. The first platform-only delivery is recorded under `output/imagegen/baemin-platform/`: the checkerboard rider is replaced by a true-alpha `1024x1024` original rider, the membership banner replaces all Korean copy with Chinese and removes an erroneous generated amount, the weekend banner receives a larger two-line Chinese headline and stronger left-side hierarchy, and a dedicated `900x1200` weekend campaign poster is connected. Homepage carousel and all three campaign Hero slots are image-only, with accessible names retained and functional rules/actions below the images. The first menu gate connects one accepted `768x768` representative image for each of the eleven merchants; their contact sheet confirms distinct shop capsules, and the fried-chicken pilot references the frozen original IP anchor. The next accepted batch connects the missing Camellia noodle and South Wind curry photographic covers, four distinct `768x768` true-alpha merchant marks, and all ten `1024x1024` true-alpha category icons. Category PNGs render above the previous font icons while retaining load-failure fallbacks and asynchronous decode. The V3 cover gate adds four `1360x640` Chinese merchant advertisements for Berry Morning, Green Basket, Good Morning Bagel Coffee, and Elm Lane Dim Sum, with compact WebP runtime derivatives; their transparent marks remain order/status identities. Existing five merchant covers still require re-review against the new per-shop capsules; `54` current generation targets remain, plus one future delayed-order illustration.
33. Harbor Roast's current uncommitted depth pass replaces its generic Menu rows with a coffee-board ordering surface and adds backward-compatible per-line temperature, cup-size, packaging, localized labels, and selected unit-price snapshots. Old cart/order records continue to normalize without customization; new variants merge only by an identical selection key. The thirteenth stable item and fourth campaign open a route-driven Pompompurin activity page with an orderable custard latte/tart combo. Seven accepted CLI-generated files are connected under stable `harbor-roast/` runtime paths: permanent paper cup, removable collaboration sleeve, separate handled carrier, exact combo, carousel, portrait Hero, and packaging story. The permanent cup never contains collaboration printing, and generated media contains no Sanrio wordmark or official logo. The prior circular Harbor detail composition is retained only as a future transferable pattern candidate. Jade Hearth's integrated paper-menu pass and Verdant Day's ingredient-level plus/minus modifiers with price deltas remain documented and deferred; Dash Grill's order-ticket candidate has advanced to the connected, user-review-pending state recorded in item 23. Focused Harbor Vitest and Playwright pass in desktop Chromium and Mobile Chrome; manual default-desktop and `393x851` checks pass crop, decode, console, and overflow gates, so all `41` Harbor PNGs are `DELIVERED_ACCEPTED`. Named physical-device proof remains separate.
34. The user-promoted Wallet currency foundation is implemented in the current tree. Built-in definitions carry exponent metadata, legacy custom definitions default safely to exponent 2, rates persist as versioned decimal strings, and Wallet exposes one exact quote/format service with explicit unavailable results. The Peach Cloud White Peach Lime price slot is its first shared-service consumer and keeps the CNY source value when a selected custom currency has no rate. Existing `amountCents` records remain untouched. Focused currency, Wallet, and Food Delivery tests cover conversion, exponent formatting, negative half-up rounding, rate revisions, backup restore, legacy rate migration, and missing-rate fallback.
35. The user-promoted Wallet card-pack baseline is implemented in the current tree. Wallet now creates six virtual single-currency accounts at ICBC/CNY, KB Kookmin/KRW, JPMorgan Chase/USD, BNP Paribas/EUR, MUFG/JPY, and HSBC Hong Kong/HKD, with one matching debit card per account, plus a Hana `Global One` credit card that supports all six currencies and bills in KRW. Institution names are real-world setting references only; SchatPhone uses no real logos or product claims, and every number, product, balance, and limit is fictional. Account/card IDs and current/default/frozen state persist through backup/restore. New transactions carry compatible account/card lineage, while older rows resolve to the default account for their recorded currency without amount mutation. Ledger records remain the only cash-balance truth, and the credit line remains outside cash and Assets. Wallet Home is now a card-first installed-app surface with transfer, receive, activity, card management, and Wallet Settings; display currency and reference rates no longer occupy the Home overview. Focused store/view tests and desktop plus simulated Pixel 5 Playwright cover seven-card rendering, six-currency credit support, legacy CNY balance mapping, card selection/default/freeze, settings/rates, CNY transfer, EUR receipt, page errors, bottom-navigation clearance, screenshots, and horizontal overflow. No separate Bank app is introduced, and named physical-device proof remains separate.
36. Role receiving-account V1 is implemented in the current tree. Non-self role profiles receive stable persisted fictional accounts; Chat's transfer action now sends a user request followed by a system-constructed verified `payee_account` card without touching the ledger. Wallet remembers the disclosed reference, verifies route/profile/currency lineage, locks the recipient and currency, lists only same-currency payment accounts, and deducts only after explicit confirmation with sufficient balance and an active debit card. Successful transfers persist receipt number, payee/profile/account/card/Chat lineage, reopen from Activity or a receipt URL, and submit one low-impact Wallet relationship fact. AI prompt policy forbids invented bank credentials and false transfer-completion claims. Formal role deletion clears disclosed payee references, while relationship reset keeps them. No NPC wealth balance, automatic exchange, background transfer, or separate Bank app is introduced.

Coordination note: `FOOD_DELIVERY_PLATFORM_CROSS_MODULE_UI_UX_PLAN.md` preserves future UI/UX and ownership direction for platform-order lifecycle, Map, Chat, Wallet, notification, and relationship integration. It is a deferred reference rather than an active slice. The live roadmap's current commerce-relevant target is one ordinary Food Delivery or Shopping action proven through existing Wallet, Calendar, Chat service-notification, and relationship-memory consequences; visual polish and additional shop concepts are unscheduled reference work.

Still incomplete:

1. Assets and Stock still need deeper product loops;
2. future ownership links from Shopping to Assets and from Stock to cue systems still need clearer rollout order.
3. service-account pushes are functional and boundary-safe, but later visual/copy polish can make them feel more brand-specific.
4. Food Delivery store surfaces now have nine structurally distinct built-in presentations: six brand-owned facades for Moon Bistro, Peach Cloud, Dash Grill, Jade Hearth, Verdant Day, and Harbor Roast, plus reusable journal, shelf, and street-route structures proven by Daylight Cafe, Sugar Lane, and River Noodles. Daylight Cafe and Harbor Roast have complete accepted media packs; Harbor's pack now includes its Captain Supply station and merchandise family. River Noodles and Sugar Lane still need `20` formal PNGs. The general `cafe_counter` and `menu_mosaic` choices remain available for user customization without fixed built-in brands. All remain subject to named physical-device review where not explicitly recorded.
5. App Store mini-app editing now has the generalized binding-target baseline, Shopping-bound generated entry support, cover facade management, installed/not-installed target-folder placement, and create-shop V0 owner handoff. A true custom Shopping store/service record model remains a Shopping-owned product decision if user-created Shopping shops need more than preset platform services.
6. Logistics/tracking share UI is reserved by the shared object contract but does not yet have a full source-app send surface. Future order/tracking screens should create `tracking_share` or `order_share` objects rather than asking Chat to infer physical-gift state.
7. Wallet primary-currency adoption is still incomplete where product semantics or media work remain undefined: Shopping mixed-currency settlement and future refund snapshots are open, and the general poster-anchor schema still needs accepted text-free media and layout work. Shopping quotations, full current Food Delivery numerical quotation, new Shopping/Food/Food Platform order snapshots, downstream Wallet quote retention, Baemin structured minimum-order enforcement, and explicit World Pack exponent authoring are implemented.

## 2. Recommended Next Slice

Roadmap 4.4 service-account continuity is complete at current acceptance. The next promoted commerce slice, when the P0 gate permits it, is the roadmap 4.9 ordinary-life consequence proof. Preserve these later candidates without treating them as an ordered queue:

1. true-device test Shopping/Food Delivery world-app, checkout, service-notification, and return-to-source flows;
2. continue Wallet cleanup/explainability and currency UX only as a focused slice;
3. exercise existing source notification plans without auto-creating Chat identities;
4. treat `补给站` and `救援调度` as trial bindings before another archetype;
5. expand Assets/Stock only through a named user loop;
6. if roadmap 4.7 approves K-pop carriers, keep marketplace/service templates separate from Calendar, Map, and Event Runtime slices.

Later product candidates remain Food Delivery store-surface polish, App Store shop management, source-owned tracking/order shares, and Shopping gift/redeem records. Promote one exact slice through the live roadmap before implementation.

## 3. Do Not Do

1. Do not let Wallet become the owner of Shopping or Food Delivery business state.
2. Do not let Assets turn into a ledger.
3. Do not let logistics become a storefront.
4. Do not let Stock absorb other finance domains without a clear decision.
5. Do not let World Pack app bindings create products, carts, orders, Wallet records, Assets records, Calendar cues, or Chat messages by themselves.

## 4. Must Sync When Working Here

At the end of a meaningful round, check and update:

1. `README.md`
2. this file
3. `PRODUCT_BOUNDARY.md`
4. `IMPLEMENTATION_WORKSTREAMS.md`
5. `docs/product-decisions/HOME_FOLDER_SHOPPING_ASSETS_DIRECTION.md`
6. `docs/architecture/RELATIONSHIP_GROWTH_EVENT_SYSTEM.md` when relationship-support semantics changed
