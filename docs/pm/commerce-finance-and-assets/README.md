# Commerce Finance And Assets Package

Updated: 2026-08-21

Use this package for Shopping, logistics, Food Delivery, Wallet, Assets, Stock, and downstream ownership loops.

Current 4.2 reminder: Wallet order-support facts may preserve Shopping/Food lineage, but they should remain supporting context inside the upstream gift or shared-meal memory rather than becoming a new primary relationship memory.

Current 4.4 note: Shopping, Logistics, and Food Delivery can push Chat service-account notifications into existing Chat Directory service accounts. These messages carry source references and route actions; source modules still own orders, logistics state, Wallet records, and route context.

Current EVE-4A correction: the Food Delivery Event Surface host and compact `Dispatch brief` are withdrawn. Food Delivery still participates in the event ecosystem through its owner Adapter: a legitimate Runtime checkpoint may produce `rider_delay` or `eta_update`, Food Delivery writes canonical ETA plus its native order timeline, and the existing Chat dispatch account carries the notification. Exact one-to-one runtime-log lineage remains; an order-screen update query cannot manufacture an exception, and no unimplemented phone/courier/customer-service choice is simulated.

Current share-card note: Shopping now has a source-owned Chat share path through `share_card`. Ordinary Shopping products shared from Chat are `product_link` objects, while only gift-card / voucher / virtual-gift-like products are labeled as direct sendable gifts. Physical products should move through product links, order shares, or tracking shares; Shopping and Logistics still own checkout, order truth, fulfillment, and delivery/signature state. The helper contract lives in `src/lib/shareable-object.js`.

Optional capability Pack note: `survival_city` can open Shopping as `补给站` through `marketplace -> Shopping`, and Food Delivery as `救援调度` through `dispatch -> Food Delivery`. Confirmed nonstandard `dispatch_board` entries use the same Food Delivery context path. These change entry wording, world context, banners, and safe view/route defaults only; Shopping/Food Delivery still own products, restaurants, menus, carts, checkout, orders, delivery events, logistics review, Wallet suggestions, and Assets suggestions.

Current Shopping pseudo-folder note: Shopping exposes eleven real-name Korean retail-reference Apps over one Shopping-owned implementation. The original six remain stable: Coupang uses `city_market`, 29CM uses `tech_catalog`, Kurly uses `fresh_market`, WORKSOUT uses `fashion_editorial`, IKEA Korea uses `room_planner`, and OLIVE YOUNG uses `care_lab`. The accepted extension adds E-MART TRADERS / `traders_club` / `member_warehouse`, CU / `cu_24` / `neighborhood_convenience`, MUSINSA / `musinsa_style` / `fashion_catalog`, BOONTHESHOP / `boon_select` / `buyer_atelier`, and Galleria Luxury Hall / `galleria_luxury` / `luxury_hall`. Their stable service IDs are additive; existing IDs remain unchanged for saved-data compatibility. The Home folder is launcher-only. Each App has its own canonical `/shopping/:serviceKey` route, identity, search, category view, favorites, cart, checkout, order history, and logistics view; no in-App switcher, cross-App cart, or mixed checkout remains. One Shopping store, schema, quote service, and persistence envelope are reused underneath with visible state scoped by `serviceKey`. Public brand references inform identity, color, and information-architecture grammar, while all 100 bilingual seed products remain SchatPhone-fictional. The original 57 stable products are preserved and 43 additive products deepen thin categories across all eleven storefronts; every store now has 8-10 products and stable IDs. Normal hydration inserts only missing built-in IDs without overwriting same-ID saved products, while explicit backup restore remains snapshot-faithful. No official catalog, product image, price, advertising copy, branch claim, or affiliation claim is imported. Formal product photography and named physical-device acceptance remain separate later slices.

Current Shopping product-photography note: Batch 0A records 12 `gpt-image-2` / `low` PNG alternatives for two existing products in each of WORKSOUT, IKEA Korea, and OLIVE YOUNG. The repository candidate package includes requests, all alternatives, manifest/hashes, contact sheets, and non-binding observations at `output/imagegen/shopping-product-photography/batch-0a-specialty-style-calibration/`. No candidate is accepted, recommended, rejected, regenerated at high quality, exported to runtime WebP, or connected to UI. The package is currently `PENDING_GIT_COMMIT`: it is visible in this checkout but unavailable to another machine until committed and pushed.

Current Shopping 29CM execution note: `docs/design/SHOPPING_29CM_PRODUCT_GRADE_EXECUTION_BRIEF.md` promotes 29CM / `nova_digital` / `tech_catalog` as the first product-grade storefront slice. Its route-owned Home, Collection/Search, PDP, Bag, Checkout, Archive, Delivery Journal, and Object Care surfaces now have a shared media registry with explicit unpublished state; actual 41-master generation and runtime publication remain gated on a usable image-generation CLI/API credential and review checkpoints.

Current Shopping visual-planning note: `docs/design/SHOPPING_STOREFRONT_BRAND_UI_AND_ASSET_GUIDE.md` remains the formal media and deep-IA planning guide for all eleven storefronts. The current code slice implements independent entry, IA, visual grammar, campaign walls, lookbook/editorial rails, service-state boards, and fictional seed catalogs for all eleven Apps. A 2026-08-21 canonical-five correction removes their repeated `header -> split Hero -> category -> four-cell nav -> product grid` silhouette: Coupang now keeps search/fulfillment/reorder above a true sticky app nav, 29CM uses a full-cover issue composition and masthead index, Kurly uses temperature lanes plus table-planning blocks, IKEA uses room/project/list utilities and a full-room stage, and OLIVE YOUNG uses ranking discovery plus a relocated routine shelf. The guide now includes an adjusted first-batch material list for the canonical five, a dedicated first-batch list for the five newer storefronts, and the complete 1493-slot eleven-store creative-master inventory while explicitly separating code-native truth from media slots. This still does not approve bulk image generation or official logo usage.

Current Food Delivery pseudo-folder note: Food Platform and shop mini apps are peer entries. Food Platform may own discovery plus checkout and order history for its own platform-internal merchants, while independent shop mini apps present their own cart, checkout confirmation, order status, delivery events, and Wallet suggestions. Platform orders must never aggregate peer shop-app orders.

Current real-shop expansion note: the clean-seed Food Delivery folder now contains exactly 15 peer launchers: Baemin, the nine prior independent shops, and Myeongdong Kyoja, London Bagel Museum Anguk, Knotted Cheongdam, Kyochon Chicken Yeoksam No. 1, and EGGDROP Gangnam Woosung. Home renders these as fixed `3 x 3` pages (`9 + 6`) with arrows/dots plus touch, pointer-drag, wheel/trackpad, and keyboard paging while containing the underlying Home gesture. Each new restaurant has a stable Food Delivery ID, four SchatPhone-authored menu items, a restaurant-scoped bag/order path, a compact runtime icon, and a stable `sourceId` pointing to one Map-owned Seoul place. Normal hydration adds only missing restaurant/menu seeds; explicit backup restore remains snapshot-faithful. Public brand and cuisine evidence informs identity and category only: no official catalog, product image, price, campaign, or advertising copy is imported. The five Map records remain Map truth and are not copied into Food Delivery order state.

Current Food Platform asset note: `FOOD_DELIVERY_IMAGE2_ASSET_PROMPTS.md` freezes the platform's consumer name as `Baemin` and records the multi-merchant photography handoff. Baemin-owned campaigns, categories, order states, and decorations may share the platform's mint-teal language; its eleven internal merchants may not. All merchant food must remain photographic, while their scene grammar ranges from pure-color editorial sets and real-food-plus-original-IP brand scenes to deliberately ordinary stall/mobile-camera photography. The `55` menu images are `11` separate five-image shop families, not one shared photography pack. Generated assets must not invent or approximate official Baemin logos or mascots. The platform code-renders the `Baemin` entry name and uses an original full-bleed mint delivery-bag icon. Delivered media now includes the true-alpha rider, Chinese membership and redesigned weekend banners, the dedicated `3:4` weekend poster, complete five-image menu families for all eleven merchants, two missing photographic merchant covers, four original true-alpha logo marks, seven original true-alpha photography-merchant order marks, ten original true-alpha category icons with Font Awesome error fallbacks, four `1360x640` merchant advertisements served as compact WebP runtime assets, and seven current order-flow illustrations connected across checkout, empty Orders, and order detail. Those four ads carry discovery identity while all eleven compact marks remain reserved for order/status identity. The menu, merchant-identity, and current order-flow gaps are closed; platform-decoration review, existing-cover re-review, and the deferred delayed-order illustration remain separate.

Current independent-shop bag note: every shop mini app has its own persistent bag within the Food Delivery-owned store. Multiple shop bags may coexist, but each facade reads, updates, and checks out only its own restaurant-scoped lines; submitting one shop removes only that shop's lines. No independent shop displays the name of, exposes, replaces, aggregates, or asks the user to recover another shop's bag.

Current independent-shop note: Moon Bistro uses the dark `dark_tray_menu` treatment for modern fine dining; Peach Cloud uses a dedicated petal-pink, pink-mist, pale-green, and charcoal `dessert_window` app adapted from exact Figma node `47:23`; Dash Grill uses an original red, mustard, paper, and ink `quick_service_chain` app; Jade Hearth uses an original rice-paper, ink-green, and cinnabar `jade_table_menu` app organized around shared tables; Verdant Day uses a grey-white, circular-photography-led `minimal_light_food` app for salads, bowls, wraps, drinks, and light sweets; and Harbor Roast uses a copper/blush/ink/cream `harbor_roast_chain` app with four campaign routes, Captain Roast IP, a coffee-menu-board ordering surface, actual hot/iced and `8 / 12 / 16oz` selection, permanent-versus-seasonal packaging choices, a dedicated Supply/merchandise route family, pickup/delivery choices, mixed cash/gift checkout, and dedicated order pages. Harbor's new Pompompurin activity keeps the permanent paper cup structurally separate from a replaceable collaboration sleeve and separate handled carrier; the generated pack uses no Sanrio wordmark or official logo. Harbor's Food Delivery-owned bean-stamp balance gates reward eligibility; successful redemption creates a zero-price gift bag line, while purchasable merchandise retains ordinary cash pricing. Peach Cloud Home restores its `3:2` horizontal brand Hero before the always-visible five-category navigation; the single region below that navigation defaults to the complete `2:3` poster carousel and swaps to the selected category's menu until the user returns to posters. Home and Discover/New remove the detached header arrows and instead expose `48px` previous/next hit zones inside each poster edge. Product posters open their own route-driven long campaign pages with a portrait Hero, two landscape story images, localized copy, Wallet-following price, and an explicit quantity CTA; the mascot poster still opens Merch. Club and Merch retain distinct campaign routes, the original-size Golden Peach advertisement remains one fixed `48 CNY` item that opens the standard detail sheet, and its three cash-purchase mascot goods share the restaurant-scoped bag and order runtime without introducing a reward store. All six remain facades over the same Food Delivery-owned cart/order runtime. Peach Cloud's delivered 39-file formal brand/campaign/poster/product/merchandise pack plus five retained legacy category marks, Dash Grill's delivered one-Hero/ten-product pack, Jade Hearth's delivered one-Hero/twelve-product pack, Verdant Day's delivered brand/promotion assets plus complete twelve-photo product pack, and Daylight Cafe's delivered one-Hero/nine-product bright-morning pack are recorded in `FOOD_DELIVERY_IMAGE2_ASSET_PROMPTS.md`. Peach Cloud, Dash Grill, Jade Hearth, Daylight Cafe, and Harbor Roast's complete `41`-file pack have completed focused desktop Chromium and simulated mobile visual review; Harbor's collaboration extension also has manual default-desktop and `393x851` crop/console/overflow evidence. This is not named physical-device proof.

Current Dash Grill UI note: Home popular items and the full Menu now use an image-led order-ticket prototype instead of the generic image/text/quick-add silhouette. Ticket numbers, section identity, detail entry, a perforated price/add footer, first-real-section defaulting, cross-section search, and visible focusable horizontal rails are connected. Detail now uses a shop-specific tray-ticket composition. The two `featured` products are complete combos whose main stays fixed while one side and one drink are selected with explicit deltas; quick-add routes into that choice, and the chosen labels plus unit price persist through restaurant-scoped Bag, checkout, backup, and order snapshots. The builder exposes `2/2` completion, a live choice summary, existing side/shake photography, branded fallbacks for pending drink media, and a compact quantity/total action bar. Crisp Chicken Tenders now routes through a required one-of-three dipping-sauce choice, and the localized sauce snapshot persists through the same Bag, checkout, backup, and Order path. All ten built-in products now follow system language with `zh-CN` as the default, accurate `en-US` retained, bilingual built-in search, and literal preservation of user-authored fields. Exact legacy seed copy migrates to the complete-meal wording while same-ID user edits remain untouched. The accepted ten-product pack remains unchanged, while two accepted `1024x1024` complete-tray photographs now represent each featured combo's default Sea-Salt Fries and Fountain Cola. Selecting another side or drink returns the large media to the honest single-main image while the option map shows the chosen components. Focused store, view, desktop Chromium, and simulated Pixel 5 coverage passes; user visual acceptance is still pending.

Current Jade Hearth UI note: its Chinese presentation name is `玉炉雅席`, while `Jade Hearth` remains the English name. The name, restaurant description, and all twelve built-in dishes now resolve through system-language presentation without rewriting the saved restaurant or user-authored menu records. Its integrated paper-banquet-menu candidate replaces the generic square-card/round-plus pattern across Home, Menu, Feast, and item detail with real menu chapters, stable dish sequence, vertical title slips, vessel-aware photography, rectangular `添菜 / Add dish` columns, shared/solo feast suggestions, and a sticky quantity/total action. Chapter and small-plate rails expose visible scrollbars and support touch, wheel/trackpad, pointer drag, arrow controls, and keyboard navigation. The accepted one-Hero/twelve-product pack is reused unchanged; no new Jade media is generated or required by this slice. Implementation is connected, while user visual acceptance remains pending.

Current Peach Cloud price-poster pilot note: one additional versioned runtime derivative removes only the baked price from the White Peach Lime poster and renders that price in its own percentage-based slot through Wallet's shared integer-minor-unit quote and formatter. CNY, EUR, and KRW formatting plus desktop/mobile bounds are covered; a custom target with no rate now keeps the honest CNY source price instead of relabeling it. Home and Discover/New both render the slot. The original poster remains unchanged, the other two posters still carry baked prices, and the derivative remains excluded from the current `39`-file formal Peach Cloud pack. This experiment does not yet provide a shared finance-wide poster-anchor schema or migrate historical records.

Current Peach Cloud campaign-page asset note: the formal pack adds six text-free WebP files under `peach-cloud/campaigns/`, bringing the formal runtime total from `33` to `39` while leaving the separate dynamic-price derivative outside that count. White Peach Lime and Waxberry Lychee each own one `1024x1536` Hero and two `1536x1024` story bands. Exact CLI requests, reference roles, PNG candidates/masters, acceptance reasons, and visual-QA evidence remain under `output/imagegen/peach-cloud-campaign-pages/`; runtime never reads that master directory.

Current prepared-shop note: River Noodles, Daylight Cafe, and Sugar Lane each have nine Food Delivery-owned menu items across four shop-specific sections and one-Hero/nine-product contracts on stable project-local paths. All three `10`-PNG packs are connected and visually accepted in desktop Chromium plus simulated mobile viewports; River Noodles keeps its river-teal noodle-shop capsule, Daylight Cafe its bright-morning terrazzo capsule, and Sugar Lane its berry/porcelain patisserie capsule. Harbor Roast keeps thirteen drinks/bakes/collaboration menu items, four stable merchandise items, a Food Delivery-owned bean-stamp balance, and an App Store identity entry. Its complete `41`-PNG formal pack is connected and visually accepted. Its exact media palette is Copper `#C67C4E`, Blush `#EDD6C8`, Ink `#313131`, Line `#E3E3E3`, and Cream `#F9F2ED`; the previous petrol/copper/cranberry capsule is retired. Harbor additionally has manual default-desktop and `393x851` evidence for the collaboration extension; named physical-device proof remains separate.

Current localization note: built-in consumer UX follows the system language and defaults to `zh-CN`, while accurate `en-US` copy remains available. Localization is a presentation concern and must preserve user-authored commerce records. Product/category wording, ingredients, icons, photography, and alternative text must remain semantically aligned in every supported language; approved fixed brand-advertising language is documented per shop.

Current Wallet currency note: WorldBook's optional capability Packs can declare and inject custom world currencies. Wallet owns the user-facing primary currency, exponent-bearing registry, USD/CNY reference coordinate, editable exchange rates, and shared exact-money quote/format service. Rate edits create persisted decimal-string revisions with provenance, and old numeric rate payloads remain readable. Shopping and Food Delivery now use the shared quote service across their promoted catalog/menu/cart paths; Chat uses the shared currency options while Stock, Assets, notifications, refunds, and other optional equivalents remain separate future consumers. Existing Wallet transactions retain their legacy exponent-2 `amountCents` meaning. No module may claim Wallet-primary conversion until both number and unit use the shared quote contract in `WALLET_CURRENCY_AND_MONEY_CONVERSION_PLAN.md`.

Current Wallet card-pack note: Wallet now presents six virtual single-currency accounts and matching debit cards for CNY, KRW, USD, EUR, JPY, and HKD, plus one KRW-billed Hana `Global One` credit card that supports all six currencies and one USD-billed American Express-reference `World Passage` global credit card. ICBC, KB Kookmin, JPMorgan Chase, BNP Paribas, MUFG, HSBC Hong Kong, Hana, and American Express are real-world institutional references for the reality-based K-pop setting; SchatPhone does not use their logos or claim their real products, and every account/card number, product name, balance, and credit line is fictional. Ledger entries remain the only cash-balance truth and resolve to an account by explicit account ID or legacy currency mapping. Credit lines are not cash or assets. Display currency and advanced reference-rate editing live in Wallet Settings and never exchange, merge, or redenominate the six account balances. Wallet remains the user-facing banking/card app; no separate Bank app is introduced.

Current Wallet card-appearance note: each of the eight payment cards owns a separate, variable-length appearance catalog. Wallet Home uses account-oriented artwork with code-rendered balances and card identity, while the card collection prefers complete collector artwork that preserves the original bank mark, chip, currency, number, and composition without balance overlays or `cover` cropping. The collection uses exactly six `2 x 3` slots per page with `All / Owned / Locked` filters, pagination beyond six designs, and intentional sealed empty slots. Seven retained complete supplementary cards remain separate from compositionally different newer backgrounds: ICBC `Hello Kitty Gift / Blue Hour / Gilded Muse / Secret Garden`, KB `Kakao City`, Chase `Peanuts Rooftop`, and BNP `Little Prince Arcade`. Owned IDs, selected appearance, and reserved progress persist through backup/restore; one appearance binds only to its declared `paymentCardId`, and changing it never creates another balance, credit line, or payment instrument. The new local collector/home WebPs still require formal image-bed publication before commit.

Current role-payee note: each non-self role profile owns a stable fictional receiving-account definition. Chat can request and disclose that account only through a system-generated `payee_account` card; creating the card does not create a Wallet transaction. Wallet persists only the disclosed reference, locks the recipient and currency, accepts payment only from a matching same-currency account with an active debit card, and creates the ledger row, receipt, and low-impact relationship fact only after explicit confirmation. This V1 does not model NPC wealth, credit the role with a spendable balance, invent bank credentials through AI text, or exchange money automatically.

Current verified-payee note: Wallet Home now exposes a visible management entry for every retained role-payee reference. The focused list presents owner, institution, masked account, currency, and verification status; every active row enters the existing role-payee confirmation workflow with an empty amount and note. Wallet-origin transfers and receipts return to the payee list, while Chat-origin account cards retain their Chat return path and Activity-opened receipts do not claim Chat origin. Repeat transfer reuses the existing same-currency account/card, balance, confirmation, receipt, ledger, and relationship-fact contracts; it does not create a second transfer runtime.

Current Wallet receipt-sharing note: a completed role-transfer receipt can create the same bounded internal Chat-share draft used by Map and Music. Wallet supplies the immutable receipt snapshot and remains the transaction owner; Chat requires recipient selection and explicit send, then attaches the receiving conversation as `returnChatId`. Opening the sent card restores the saved receipt and returns to that receiving conversation even when the transaction's original `sourceChatId` points elsewhere. Sharing, opening, canceling, and returning never mutate the ledger, balance, receipt, or relationship fact.

Current ordinary-life proof note: one Shopping gift-card order now has desktop Chromium and simulated Pixel 5 Playwright evidence across its existing user-confirmed consequences. The user explicitly creates the matching Chat service account, places a gift order for an existing role, receives the source-owned order notification, confirms the delivery cue into Calendar, completes the Shopping order, records the expense into Wallet, and sees the Shopping gift plus Wallet supporting fact consolidated into one Contacts memory. The flow creates neither a Chat identity nor a Wallet transaction automatically, preserves `orderId` lineage across records, adds no relationship metric delta for Wallet support, and stays idempotent after reopen.

Current commerce-quote note: Shopping catalog/cart values and Food Delivery menu, modifier, delivery-fee, platform-campaign, cart, and checkout values now show Wallet-primary quotations while retaining source prices and honest missing-rate fallback. Food Delivery custom menu records inherit their restaurant's source currency, Baemin minimum orders are structured KRW/CNY Money values and enforced before checkout/submission, and all independent-shop facades preserve source customization values before quotation. New Shopping, Food Delivery, and Food Platform orders persist normalized quote provenance through backup/restore, and explicit Shopping/Food Delivery Wallet expenses reuse the source order snapshot instead of re-quoting. World Pack currency authoring exposes exponent `0-6`. Mixed-currency Shopping settlement, future refund snapshots, and a general poster-anchor schema remain open.

Current Wallet quote-explainability note: every Wallet Activity row now opens a route-backed general transaction detail. Valid saved snapshots show source money, settled quoted money, the exact recorded rate, `rateSetId`, localized rate-source provenance with its saved key, and quote time without calling the current quote service. Legacy or malformed records show an explicit no-snapshot state, unknown historical currencies fall back to raw minor units and code, and role-payee receipts remain a separate `receiptId` surface. Current Wallet currency/rate edits do not change an open or reopened historical detail.

Current Wallet Activity search note: the existing Wallet/Chat/purchase source filters now compose with a case-insensitive search over recorded transaction identity and provenance, including title, counterparty, note, currency, source record, receipt, account/card lineage, and amount. Search changes only the visible Activity projection; it never rewrites ledger rows or historical quote snapshots, and every result retains the same general transaction-detail entry.

Current Wallet monthly-statement note: Activity exposes a focused month page over retained ledger records. It lists available local calendar months newest-first and reports income, spending, net, and record count separately for every original transaction currency. It never aggregates unlike currencies or uses current rates. Statement rows are read-only projections that retain the general transaction-detail path and return to the selected month; deletion and role-receipt actions remain on Activity.

## Read This Package In This Order

1. `STATUS_AND_HANDOFF.md`
2. `WALLET_CURRENCY_AND_MONEY_CONVERSION_PLAN.md` when working on money values, exchange rates, price formatting, or finance-consuming modules
3. `FOOD_DELIVERY_SHOP_MINI_APP_HANDOFF.md` when continuing Food Delivery pseudo-folder or shop mini app work
4. `PRODUCT_BOUNDARY.md`
5. `IMPLEMENTATION_WORKSTREAMS.md`

Also read when needed:

- `FOOD_DELIVERY_PLATFORM_CROSS_MODULE_UI_UX_PLAN.md` when a later architecture or execution slice promotes Food Platform order integration; it is a deferred coordination reference, not the active roadmap
- `docs/design/FOOD_DELIVERY_SHOP_BRAND_UI_DIRECTION.md` when reviewing shop identity, card differentiation, interaction defects, asset gaps, or confirmed visual follow-up; its audited register is a design reference, not an execution queue
- `docs/product-decisions/HOME_FOLDER_SHOPPING_ASSETS_DIRECTION.md`
