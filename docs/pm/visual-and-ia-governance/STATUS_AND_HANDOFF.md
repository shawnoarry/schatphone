# Visual And IA Governance Status And Handoff

Updated: 2026-08-26

This file is the handoff page for visual hierarchy, information architecture, and rebuild-vs-polish decisions.

The sequenced visual-polish working plan (completed slices, the current Calendar/Reminders lane with the marker-system decision record, and the unscheduled backlog) now lives in `docs/pm/visual-and-ia-governance/VISUAL_POLISH_TODO.md`. It is a package-owned working plan, not a second roadmap; `docs/roadmap/TODO_ROADMAP.md` still owns priority.

## 1. Current Status

Status: `PARTIAL_DONE`

2026-08-17 Gallery token-parity slice: `GalleryView` now renders entirely from `--system-*` tokens instead of hard-coded light values, so Photos follows both `default` and `zen`; the asset-library empty state is an intentional icon/title/hint/primary-action block with a filter-reset recovery; the page now uses one scroll region under the persistent topbar so the albums card and photo grid remain reachable on phone-sized viewports (previously the controls region exceeded the viewport and the grid area collapsed to ~32px); icon buttons and all selects have accessible names; and the shared Playwright visual gate now covers Gallery alongside Home, Settings, and Appearance. Gallery People/Photos-first entry work remains deferred as recorded below.

2026-08-17 Camera settings-family token-parity slice: `CameraSettingsShell` and all seven Camera settings views (Settings index, Tasks, Providers, Provider detail, Defaults, App Routing, Diagnostics) now render from `--system-*` tokens instead of hard-coded light surfaces, borders, grays, accent blues, and semantic status colors, so the whole Camera settings family follows both `default` and `zen`; category icon tiles stay as the app-owned iOS-style decorative palette, and the dark summary/provider-mark tiles now invert adaptively (`--system-text` ground with `--system-text-inverse` glyph). The Camera capture surface keeps its intentional always-dark app identity unchanged. Camera true-device evidence and prompt-assistant management UI remain separate as recorded below.

2026-08-17 TTS voice settings Chat-alignment slice: `TtsSettingsView` now renders from the `--chat-*` shell variables instead of its one-off hard-coded green light theme, because the page is entered through Chat Settings and its visual owner is Chat. The header now uses the shared `chat-native-header`/`chat-native-back` pattern naming its actual return target, and the provider segmented control, cards, fields, badge, and preview CTA follow the active Chat appearance: Kakao yellow by default, blue under iMessage layout, green under WeChat layout, and any user Chat theme/custom CSS from now on. Chat appearance presets intentionally stay light under the `zen` system theme, so this page now matches Chat itself in every mode. TTS provider quality smoke, Chat read-aloud, and durable voice messages remain separate roadmap 4.15 gates.

2026-08-17 hosted-product-proof asset hardening: the Playwright evidence path now retrieves real project CDN bytes with bounded timeout/retry, completed-response caching, request-context-scoped in-flight de-duplication, settle-before-fail prewarm batches, and fail-closed route fulfillment. Food Delivery, Shopping, and Map proof specs use explicit approved packs where assets are mounted after interaction; lazy campaign media is scrolled into view before visual assertions. The scoped 34-test desktop + simulated Pixel 5 Chromium focus set passes with both two-worker and default four-worker execution. This remains local/simulated evidence only: hosted deployment, installed-PWA/relaunch, and physical-device acceptance remain separate release gates; no hosted or true-device claim is made here.

What is already landed:

1. this package now exists as the place to separate structural IA issues from simple styling issues;
2. several functional lanes already have clearer structure baselines, such as map-first Map and safer in-app confirmation/dialog patterns;
3. the product direction is explicit that current UI is still functional scaffolding in many places, not final visual immersion.
4. Contacts detail now has a first-pass Role Hub overview that separates L0 summary from deeper profile, relationship, memory, and danger-zone sections.
5. Contacts detail item sections now use grouped Manual details vs Event-attached presentation, with linked-memory entry points for event-attached items.
6. Contacts memory detail now exposes a small source-audit layer with per-module cards and supporting-event drill-down, making relationship-source provenance readable without moving ownership away from source modules.
7. Contacts detail now also supports inline editing for manual role-detail items and a visible linked-activity list below the summary card, making the page feel more like an active role-management hub.
8. Contacts memory presentation now includes a lightweight review toolbar and selected-memory headline facts, so memory management is no longer only a flat list-plus-delete flow.
9. Contacts memory review now includes lifecycle status and note management, making the role hub feel functionally complete enough to leave 4.1 and move on.
10. Home now has a product-decision baseline for a template-slot desktop layout system in `docs/product-decisions/HOME_DESKTOP_LAYOUT_SYSTEM.md`.
11. External visual reference assets are documented as a cross-PC confirmed path in `docs/references/VISUAL_ASSET_LIBRARY.md`.
12. Home now has a first-pass neutral template library: six abstract 4-column x 6-row layouts, per-formal-page template selection, explicit slot placement records, edit-mode grayscale slot previews, local slot content replacement, and a lightweight edit-mode content library for unplaced entries.
13. Home slot editing is now content-type mixed: apps, folders, built-in widgets, and custom widgets can be placed when they fit the selected slot. Widget Center remains the widget creation/import and edit-entry surface; Appearance may later expose broader template management; App Store-like surfaces should remain app-entry visibility management only.
14. Custom widget actions now have a first-pass functional loop: Widget Center stores optional click action metadata outside imported code, Home executes whitelisted app/system targets in normal mode, and edit mode still uses taps for slot editing.
15. The first cross-entry visual pass is landed for Home customization: `主屏 / 桌面 / Home` is documented as a system desktop layer, not a visible app; `组件 / Widgets`, `外观 / Appearance`, and `应用商城 / App Store` now expose coordinated UI entry points into desktop editing without making App Store behavior a real download store.
16. `更多 / More` has been retired as a Home app. Its useful responsibilities were merged into clearer owners: `应用商城 / App Store` owns app-entry visibility, `外观 / Appearance` owns the Home smart panel display switch, and `设置 / Settings` owns lock-screen focus mode.
17. `外观 / Appearance` root now has a first native-system control-surface pass: a current-look overview, Home desktop template preview, and grouped Theme/Font/Icon/Widget entries. The deeper Theme, Font, and Icon subpages also have a first token-coverage pass to reduce white-card and hardcoded-blue leakage in the dark system theme.
18. Home Today View copy now uses native-system language for optional entries and smart summaries instead of developer-facing install, hidden-system, fixed-placeholder, or lab labels.
19. Widget Center has a regression test that keeps style starter cards as lightweight thumbnails rather than iframe previews; live iframes remain reserved for draft and saved custom widget previews.
20. Home edit mode has been consolidated around fixed slots: template selection and the unplaced-content library are on-demand, Dock Widgets long-press enters the current page's edit mode, slot replacement candidates are exact-size filtered so app/folder entries stay in `1x1` slots, and incompatible overflow entries no longer render inside the template grid.
21. Home template switching now behaves like a new-device layout setup: when a page changes to a template with fewer or different slots, unmatched entries leave the visible desktop and return to the recoverable library instead of being auto-filled into the new layout. Slot replacement can browse the full compatible app/widget library and marks whether an item is already on another Home page, in the Dock, or only in the library.
22. Unversioned persisted Home desktop layouts now migrate once into the cleaned setup layout on hydration, so old local browser state does not keep showing the previous crowded Home arrangement after the template-slot model changes.
23. Widget Center now has a more visual market/custom/import pass: official widget styles render as effect thumbnails with size filters and preview sheets, custom widget creation leads with starter thumbnails and live previews, saved widgets render as preview cards, and import stays a visual-code library step instead of placing content directly on Home.
24. `应用商城 / App Store` now has a first standalone simulated-store management pass: search, category filters, selected app detail, open/add-to-Home/remove-from-Home actions, protected App Store entry handling, empty search state, and a route into Home slot edit mode with the selected hidden app ready for placement.
25. Home edit mode now separates opening the recoverable content library from selecting a specific item. Library-open-but-unselected keeps slots tappable for size-specific replacement, while selecting an item lights only compatible slots; slot picker filters now hide content-type tabs that cannot fit the selected slot size.
26. Home Today View now has a fixed `1x1` recovery-entry template: App Store is always reachable there, World Hub and future Cheats remain conditionally fixed system entries, and reserved slots are visible without making the `-1` screen a selectable formal Home layout.
27. WorldBook now has a state-first IA baseline inside Settings: stable current-world-setting overview, separate Optional capability Packs activation, service-template confirmation, Setting Text links, profile templates, knowledge management, and advanced compatibility appear in a clearer L0 -> L3 order. The page uses a single-focus workspace so Pack identity never replaces world identity.
28. Book now has a V1 app-like text-library surface with library list, read-first detail view, guarded editing, import/export controls, and WorldBook source usage state.
29. WorldBook's Setting Text panel now has a phone-testable picker with whole-document/selected-section activation, changed-text warning, and refresh action. Setting-text picking and changed-text review now open as layered sheets instead of expanding inline inside the Settings page.
30. WorldBook's Optional capability Packs panel now shows active world-app entries, and Shopping has the first world-app banner/action pattern for `补给站` that explains ownership boundaries before normal Shopping controls.

31. WorldBook's Optional capability Packs and Knowledge panels now have a first internal craft pass: pack effects use clearer icon cards, knowledge starts with status metrics, and knowledge create/edit work opens in a focused bottom sheet instead of stretching the Settings page inline.
32. WorldBook's Setting Text panel now has a user-facing control surface: the primary view says which Book setting text AI will read, active text cards have status badges, and unused or missing text is kept in Advanced management so disabled/review states do not compete with the current-context view.
33. WorldBook's old Kernel panel is now Advanced compatibility: a short base-worldview note only for cases with no active Book text. Templates uses preset/world stats plus stronger template cards.
34. Book now follows a native-app IA split on phone-sized screens: open Book into the source shelf, tap a text asset to enter its read-first detail page, and open long-form editing as a root-level guarded sheet instead of an inline editor trapped inside the reading page.
35. App Store now follows the same mobile progressive-disclosure rule: the catalog list is the main view, and app details open as a root-level sheet on phone-sized screens while desktop keeps the list/detail management layout.
36. Widget Center now applies the same phone IA rule: library/starter/created widgets remain the scan-first page, while custom editing and import JSON entry open as focused mobile sheets. Widget code/import work should not be stretched inline across the whole phone page.
37. Appearance Theme now starts applying the same native-system execution-panel rule: phone-sized screens keep theme/wallpaper status scan-first, while wallpaper-source selection and advanced CSS editing open as focused sheets instead of staying inline in the main scroll.
38. Appearance Font and App Icons now follow the same rule: font presets and current app-icon states remain scan-first on phone-sized screens, while custom font-stack editing and per-app icon/accent edits open as focused sheets. Built-in app glyphs are also included in the icon preset list so default icons do not render as blank select values.
39. Chat App now has the first Kakao-like entry-ownership split for this visual pass: bottom `Me` is the identity/social surface, the gear opens Chat Settings, and Chat Appearance owns Kakao/WeChat/iMessage layout modes plus Chat-scoped CSS without moving global Appearance CSS. The latest polish keeps that IA intact while making Messages, Me, and Chat Appearance feel more phone-native: yellow Chat entry chrome plus white message sheet, active bottom-tab states, Me compact social stats/recent avatars, and visual layout thumbnails for row-mode selection.
40. World Pack direction is clarified for the next lane: activation remains in Settings -> WorldBook, but the effects must not stay trapped there. Active packs should provide a global world UX package for existing apps and unlock world-specific app entries into Home/App Store/App Library entry management.
41. The first World Pack global app-entry unlock seam is landed: active pack app bindings show as World entries in App Store, can be routed into Home/App Library placement, and open target apps with `worldPack`/`worldApp` context. Activation/review still belongs to Settings -> WorldBook.
42. The first World Pack target-app UX seam is landed: `world-pack-app-bindings` resolves active-pack labels, terminology, accent, route query, and boundary copy. Food Delivery shows dispatch world-app hero/banner plus safe Nearby default without taking over food-order workflow, Calendar shows reservation context without taking over schedule/push ownership, and Map shows transit context without taking over trip/location truth.
43. The Appearance / World Pack layering direction is clarified: World Pack supplies default immersive treatment, while user customization remains a higher-priority explicit override layer. `src/lib/app-shell-scope.js` provides stable `data-app`, `data-route-scope`, `data-world-pack`, and `data-world-app` hooks, and persisted scoped CSS remains runtime-compatible. The current global Appearance surface no longer authors app/world-app scoped CSS, and global Appearance packs export only global portable fields; app icons, app skins, scoped CSS, Home layout/widgets, and Chat appearance stay with their owners.

44. WorldBook's Optional capability Packs panel now exposes the nonstandard-app proposal review UI: AI extraction or pasted JSON can be reviewed against the built-in whitelist, loading/empty/error states are explicit, rejected suggestions show why they were blocked, and confirmable suggestions require an explicit add-to-pack action before they become appBindings. Confirmed entries now carry world-pack/target-module metadata in App Store detail, can be placed from Home's library, and open their target app with world context; dynamic `transit_pass -> Map`, `reservation_board -> Calendar`, and `dispatch_board -> Food Delivery` paths have regression coverage.
45. The Optional capability Packs surface now separates activation/status from global entry management: it shows the active world-app snapshot, tells users that browse/place/open flows live in App Store's `World` section, does not provide an App Store jump button inside Settings, shows service-account template availability as a Chat handoff, and keeps nonstandard proposal review as an advanced/collapsible area. `black_market` is blocked as `needs_dedicated_app` and does not masquerade as a Shopping world app.
46. Home edit mode now preserves screen navigation: page dots stay usable during editing, empty-area swipes can switch screens, and Widget Center placement flows no longer trap users on the entry screen.
47. Appearance now has a global-only ownership baseline: the root no longer promotes App Icons or Widget Center, Advanced CSS is global-only, and appearance packs exclude app icons, app/world-app scoped CSS, widgets, Home placement, and Chat appearance. App identity and app-specific skins are reserved for App Store/app-owned follow-up slices.
48. App Store now owns app identity/icon editing: each app detail can open a focused `图标与外观 / Icon & Appearance` sheet, choose a built-in glyph/accent, choose a Gallery image, upload a local image into Gallery for icon use, preview before saving, and restore default. Home, Dock, App Store, and supported notification surfaces resolve the same app identity.
49. App Store now owns app skin editing for standard route-backed apps: supported app details can open an `APP 皮肤 / App Skin` sheet, choose a built-in skin preset, save CSS scoped only to that app's shell route, or restore default. Chat remains excluded from this generic editor because Chat Appearance is still the deep owner for chat layout and Chat-scoped CSS. Global appearance packs continue to exclude and preserve `appSkins`.
50. The App Store icon flow now has desktop and mobile E2E coverage proving a Gallery image icon can be selected, saved, and rendered back on Home.
51. App Store World entries now have a product-facing handoff card in both desktop detail and the mobile detail sheet: the card tells users which World Pack created the entry, which target app will open, and that App Store only manages placement/launch while WorldBook keeps activation, review, and source ownership. The handoff is covered by App Store unit tests and Home entry E2E before the route opens with `worldPack`/`worldApp` context.

52. Food Delivery has its first platform-to-store IA baseline: the platform lists categories/restaurants, each restaurant can open as a route-query store mini-app, and store surfaces can vary visually by restaurant category while shared business logic stays untouched.
53. The App Store / pseudo-folder entry taxonomy is documented in `docs/product-decisions/APP_STORE_ENTRY_TYPES_AND_FOOD_SHOP_APPS.md`: App Store should manage independent apps, World Pack app entries, reusable folder mini apps, and system entries as user-visible launch entries while preserving backend ownership boundaries. `Shops` is the shop-shaped subset of folder mini apps with limited target-folder bindings, not a Food Delivery synonym and not an App Store browsing category.
54. Food Delivery now reflects the shop-entry taxonomy in-app: the default Food surface is entry-first, with categories demoted to filters and restaurants presented as shop mini-app launch cards.
55. Moon Bistro now proves the Food Delivery shop mini-app direction with a dark tray menu: raised circular dish areas embedded in dark menu cards.
56. App Store now has the V1/V2 entry-management taxonomy in code: filters split `Apps / World / Mini apps / System`, detail panels show runtime identity and ownership boundaries, standard apps can save phone-facing display names without changing backend ids/routes, and Food Delivery restaurants appear as `food_delivery`-bound folder mini apps that open with `restaurantId` context without becoming Home-placeable independent apps. Folder mini apps can save App Store-side display/icon overrides, cover facade media, short descriptions, display tags, shop-template selection, explicit target folder, and installed/not-installed folder placement; Food Delivery reads presentation/install-state fields for launcher/store display while restaurant records, menus, carts, orders, Wallet handoffs, Map handoffs, and Chat service pushes remain source-owned. Shopping platform services now appear as `shopping`-bound folder mini apps that open Shopping with service/shop-entry context, render App Store cover facade media, and hide from the Shopping folder list when uninstalled while products, carts, orders, logistics, Wallet/Assets handoffs, and service pushes remain Shopping-owned. App Store `Add mini app` is now a target-folder handoff sheet: it routes Food Delivery or Shopping with `createShop=1`, and the target app shows the ownership handoff before source-owned creation/editing. App Store does not own shop favorites/recent lists, consumer category filters, or commerce browsing organization.
57. Food Delivery menu-item interaction is now layered: the dish card opens a detail sheet for description/base ingredients/add-to-cart, while the small edit icon inside that sheet switches to single-item copy/image editing.
58. App Store folder mini-app flows now have mobile-sized E2E coverage: `e2e/app-store-mini-app-flow.spec.js` verifies the App Store -> Add mini app -> Food Delivery/Shopping owner handoff, existing Food Delivery and Shopping shop-entry opening, installed/not-installed pseudo-folder placement, source-record retention after hiding, and no horizontal overflow in the simulated phone viewport. This substitutes for part of phone QA when real-device testing is unavailable, but it does not replace later true-device checks for touch feel, browser chrome, keyboard, weak network, and safe-area behavior.
59. WorldBook Setting Text UX has been clarified after product review: user-facing copy should avoid backend words like "source" and should not present Settings as the worldbook editor. The first panel answers "which Book text is AI reading now"; user choices are treated as the current world background; advanced maintenance only covers unused or missing text references; changed active text stays visible in the active list with a confirm-new-version action. Base-worldview copy belongs only under Advanced compatibility as a short fallback when no Book text is active.
60. WorldBook now has a `世界设定工作台 / World setting workspace`. It presents Setting Text, structured encyclopedia entries, Profile Templates, Capabilities and Apps, and Compatibility fallback as parallel layers instead of an intended product progression. Every layer opens the existing focused panel, and the workspace makes Book writing/storage plus WorldBook activation one visible workflow without automatic links.
61. WorldBook Profile Templates now includes a product-facing Contacts handoff. The panel tells users that WorldBook defines world-specific profile fields, while `通讯录 / Contacts` fills concrete values for roles, self profile, and NPCs; the action opens Contacts with `from=worldbook&focus=profile_templates`, and Contacts recognizes that route with a focused entry note without making WorldBook own person-value editing.
62. Contacts role detail now completes the V1 handoff loop with an inline `世界字段 / World profile fields` editor. It keeps the visual ownership language consistent: choose a current-world WorldBook template, fill this person's concrete values, set visibility, and save back to Contacts-owned `templateLink/profileValues`.
63. The WorldBook Profile Templates -> Contacts focused handoff now has a simulated phone-viewport product check: copy an ABO preset, open Contacts from the handoff, create a role profile, fill `世界字段 / World profile fields`, and verify the saved value renders without page errors, horizontal overflow, or page-level mojibake. E2E also passes with 14 tests across desktop and mobile Chromium.
64. Contacts now has a first phone-like entry refactor: Search, My Profile, Recent interactions, Main Roles, and NPC / World Roles are ordered like a real contact app. Recent interactions is an avatar shortcut row and does not replace the complete role/NPC lists. The simulated mobile E2E covers order, shortcut navigation, search filtering, and no horizontal overflow.
65. Contacts world-field editing now presents worldview-template fields through stable visual controls: choice chips/selects, long-note areas, tag entry with preview chips, and person-reference guidance. This keeps dynamic world-specific template content readable without creating per-world custom screens.
66. Contacts template-change review now appears inside the world-field editor before saving. It explains which fields update, which old fields stay as custom values, and keeps the action inline instead of using a blocking modal.
67. Contacts AI world-field drafting now appears as a secondary inline editor action. It drafts empty fields only, reports applied/kept counts below the editor actions, and preserves Save as the clear user-controlled commit.
68. Contacts current-world template adaptation now uses a soft review card in role detail when a profile uses an unavailable, older, or other-world template. The card recommends a current-world template, explains carry-over/custom-preserved counts, and opens AI draft adaptation inside the existing editor so users can still review before saving.
69. The full WorldBook Profile Templates -> Contacts world-field value loop is now protected by E2E: copy a world template, enter Contacts from the handoff, create a profile, fill and save world fields, and verify the phone-width layout has no horizontal overflow or page errors across desktop and mobile Chromium.
70. Book and WorldBook now close the first-use source gap for the K-pop trial world: `现代首尔 K-pop 娱乐圈` appears in Book as built-in read-only setting text, WorldBook can activate/deactivate it from Setting Text, and editing the built-in text creates a user copy instead of making Settings feel like the worldbook editor.

71. WorldBook's Setting Text picker now keeps library browsing in-place: opening the text library shows Book cards inside the current sheet instead of navigating to Book. Book remains the editor; WorldBook remains the activation surface.
72. Book/WorldBook now have real built-in placeholder manuscripts for testing interaction breadth: encyclopedia, profile-template, world-pack-reference, and reference-material entries appear as normal Book-card choices instead of mock UI-only cards.
73. WorldBook's Book-card picker now groups manuscripts inside category/role project cards instead of showing one flat mixed list, making worldviews, rules, and encyclopedia entries easier to scan. Profile templates stay in the dedicated structured template area instead of appearing as Book text categories.
74. WorldBook's Active World overview now acts as the main text-context control surface: it shows active context text character count plus selected manuscript name chips, then opens focused directory sheets for Worldview, Rules, Encyclopedia, and Profiles. Backend reference-style labels are kept out of this primary path.
75. Lock-screen notification cleanup and Settings Software Update V1 are landed. Lock cards now support per-item clear plus clear-all, Settings has a Software Update entry/subpage with current version, available version, release notes, check/install/later/restart actions, and update notifications route to `/settings?menu=software-update`. Plan/result: `docs/superpowers/plans/2026-06-09-lock-notifications-software-update-plan.md`.
76. Moon Bistro's Food Delivery shop template now has its first local-asset visual polish: the shop header uses a candlelit cover from `public/images/ui-assets/apps/food-delivery/moon-bistro/`, initial menu dishes use local food photos, the dark tray cards have stronger embedded circular image depth, and app-local public image URLs are preserved by the shared image-source contract so visual assets can live in the repo while user-edited dish images remain replaceable.
77. Moon Bistro's shop menu now has a sectioned browse pattern: nine seeded local-photo dishes are grouped behind a sticky side rail, empty cart and support panels stay hidden until useful, and the first screen reads more like a real independent shop mini app than a Food Delivery backend/status page.
78. Network & API setup now follows a URL-first IA: the page no longer presents OpenAI official, OpenAI-compatible gateway, and Gemini as first-level choice buttons. Users enter the endpoint directly, the app auto-detects the transport type, and saved API configurations are loaded from the dropdown near the URL field. Provider templates remain a helper-level code concept only and should not dominate the visible setup path.
79. Network & API URL-first IA now has broader transport affordance coverage: common OpenAI-compatible base/model/chat paths, OpenAI/Azure Responses paths, Anthropic Messages paths, Gemini native paths, Azure deployment URLs, and local/server-auth gateway endpoints can be entered directly. The API Key step may become optional after URL detection; the visible dropdown is for saved user configurations, not provider-brand selection.
80. Network & API setup now keeps the necessary functional loop compact: URL, key, model input/selection, model-list refresh, connection test, and save-current-settings live in one primary connection panel, while saved-configuration management and diagnostics are secondary disclosures. The page should not return to one large stacked block per capability.
81. Visual governance now has a minimal engineering gate: a visual round uses zero or one specialist skill, while `npm run test:visual` checks Home, Settings, and Appearance in day/night plus desktop/mobile, attaches screenshots and axe reports, and blocks page errors, horizontal overflow, and critical accessibility violations. The first run also fixed missing accessible names on Home Dock entries and the Settings backup-asset checkbox.
82. Modern Seoul K-pop Book/WorldBook carrier correctness now has a promoted 2 + 6 baseline: two core texts and six merged encyclopedia manuscripts are independent built-in Book assets; WorldBook presents and persists only explicit per-manuscript choices, allows arbitrary encyclopedia subsets including zero, and creates no dedicated K-pop World Pack or automatic core-to-encyclopedia binding. Retired small-draft IDs remain hidden compatibility lookups for existing links.
83. The first focused K-pop schedule-scene content slice extends the public catalog to 2 + 6 + 1 with one independent `K-pop 音乐节目打歌日小剧场规则` world-rule asset. It consolidates applicability, inputs, scene beats, role duties, public/private boundaries, continuity, variants, anti-fabrication rules, and future mini_scene semantics without adding a World Pack, automatic source link, module trigger, renderer, or Calendar/Map/Event Runtime schema.
84. Roadmap 4.8 now has an architecture-accepted shared Mini Scene UI direction. Settings will eventually expose explicit registered-module off/text/interactive choices and world-profile binding; Book will edit separate structured transform profiles; Text and sandboxed HTML Presenter Adapters will share accessible modal/fallback behavior. The current K-pop prose rule remains content only, and no Settings/popup/editor/presenter implementation is landed.
85. Book and WorldBook now complete the first unified world-setting-workflow slice. WorldBook exposes stable world identity, active text, the full Book catalog count, review issues, and parallel optional setting layers, then hands writing/import work to Book with an explicit return path. Book exports each manuscript as strict versioned/lossless `.worldbook.json`, editable/re-importable `.md`, or portable body-only `.txt`; export never changes WorldBook activation.
86. The existing World Pack closed loop now has focused Playwright evidence across desktop and simulated-phone Chromium. `e2e/world-pack-loop-validation.spec.js` starts with zero enabled Packs and zero selected Book/encyclopedia content, explicitly activates a reviewed capability Pack in WorldBook, verifies App Store World entries, places four existing target archetypes through Home, refreshes persisted placement, launches and returns from Shopping, Food Delivery, Calendar, and Map, then disables the Pack and verifies safe fallback. The same journey covers long Chinese/English labels, horizontal overflow, page errors, and critical axe failures; the evidence produced focused Shopping/ImageSourcePicker/Map accessible-name fixes and Food Delivery long-title wrapping. No real phone or remote-debug device was available, so true-device acceptance remains incomplete.
87. Roadmap 4.9's first successful Chat activation loop is complete at automated desktop and simulated-phone acceptance. Chat now turns incomplete provider setup into an explicit Network & API action while preserving the originating thread and draft; Network offers Continue Chat only after the current settings are saved and the same configuration passes its smoke test. Focused Playwright proves the first manual reply, bounded return context, responsive overflow, page-error, critical accessibility, and fake-credential visibility guards. Hosted-provider and true-device proof remain separate release gates.
88. Roadmap 4.9's explicit custom role -> Chat journey is complete at desktop and simulated-phone Chromium acceptance. Contacts now offers a direct `Start Chat` action for eligible Main Role/NPC profiles, reuses existing targets, preserves bounded Contacts context through Network recovery, and returns to the same profile after the first manual reply. The focused fixture intentionally starts from an explicit empty custom-role snapshot and does not redefine or remove the seeded fresh-storage roles; zero Book sources, encyclopedia entries, and extra World Packs remain valid.
89. Peach Cloud's installed-app navigation now has consistent page depth: its fixed Home, Search, New, Bag, and Orders entries switch mutually exclusive `shopView` pages, submitted orders open a dedicated progress detail, and empty Bag/Orders states remain real pages. The generic Food Delivery cart and support blocks no longer extend the Peach Cloud home, while Food Delivery continues to own the shared cart, checkout, order, and event state.
90. Commit `5bd7003` landed the independent-shop cart-ownership guard and Peach Cloud progress correction. Foreign, mixed, or unknown carts fail closed instead of being rendered or checked out as the active shop's bag; replacing a bag requires explicit confirmation, and the accepted replacement writes the target item once. Peach Cloud maps cancelled to no active progress and placed, preparing, delivering, and delivered to stages 1 through 4. This work did not expand Store or schema ownership.
91. Commit `7194f1c` landed Moon Bistro's consumer-facing `Order & delivery` pass. The surface stays folded by default and presents delivery details, `Check for update`, `Confirm delivery`, `Remove from history`, explicit `Save to Wallet`, and the Map snapshot through existing Food Delivery handoffs rather than diagnostic ownership copy. It preserves stable route-detail hooks, safe pending ETA fallback, 44px controls, visible focus, long-copy wrapping, and reduced-motion behavior.
92. Food Delivery's current finish boundary is explicit: Moon Bistro is close to product-ready, while Food Platform checkout/order surfaces still expose diagnostic placeholders where formal order-state artwork is missing. Peach Cloud's image-identity gate is complete with three independent brand/campaign scenes, seventeen unique title-matched product photographs, and a dedicated peach/cloud mascot mark. Desktop and simulated-phone browser evidence is available, but simulated mobile is not true-device acceptance.
93. Two failed ImageGen drafts never entered product assets or code. The generation failure was traced to a Windows `.cmd` invocation truncating a multiline prompt passed inline, while a compatibility image service also returned non-square output despite the requested square size. Any later CLI trial must pass the Chinese prompt through a UTF-8 prompt file and inspect the dry-run request payload before generation; generated output remains a candidate until normal visual review accepts it.
94. Vistack is a validated candidate for a semi-automatic asset workbench, not a configured project capability, approved production toolchain, formal asset source, or second roadmap. A later controlled trial may send a prepared asset list to Vistack, download outputs into a temporary intake area, use human selection and quality checks, and wire only accepted files in a separate implementation slice.
94A. WorldBook now exposes an approximate enabled-setting-text token cost in the current-world overview, split by setting-text category. The estimate is informational only, never limits or removes text, and remains separate from Chat's conversation-specific next-request estimate.
95. Roadmap 4.10's first Camera slice is implemented. Camera is a Home/App Store installed app with a restrained dark capture screen; provider URL/key/model/proxy, defaults, module routing, diagnostics, and tasks live in Camera-owned push-navigation pages. The embedded Gallery reference sheet preserves Camera visual ownership, candidates require explicit Keep before Gallery durability, Download/Keep/Discard remain distinct, and desktop plus simulated Pixel 5 Chromium checks cover route flow, overflow, page errors, and provider privacy. Gallery People curation, automatic person/intent resolution, source-module callers, true-device validation, and hosted-provider smoke remain separate work.
96. Peach Cloud now has a persistent branded shop bar, an icon-only return-to-originating-Home action, a brand-led Home, a discovery-focused Search page without a visible catch-all category, and a release-timeline New page instead of three similar product grids. Its 17 built-in menu records use semantically matched fresh-fruit, frozen, tea/coffee, bake, and seasonal categories while preserving user-edited same-ID fields; Fresh Fruit now has five products and Seasonal has three without adding automatic date rules. The Food Delivery folder launcher and shop bar use the dedicated peach/cloud mascot mark. Five colorful background-free Peach Cloud IP illustrations replace the mismatched black line icons, while the original generic SVG set remains unchanged for reuse. Search presents the five filters in one compact row with short visible labels and full accessible names instead of a clipped horizontal rail or tall two-row container. New keeps the featured product out of its repeated timeline, prioritizes the remaining seasonal additions, and uses accurate bilingual category labels. The Home `30% OFF` block keeps the approved English campaign copy while showing the complete right-side campaign artwork without cropping. Product details, bag, checkout, and order copy follow the system language with Chinese as default and accurate English retained; approved English advertising copy remains fixed. The delivered 26-file active pack gives Home Hero, promotion, New, and all 17 products separate stable assets. Current D-drive evidence covers desktop and simulated Pixel 5 Chromium with no horizontal overflow or broken required images, plus Bag, checkout, order detail, order-list, and reload persistence. True-device acceptance remains separate.
97. Default Home release curation is complete. Normal mode defaults to three formal pages: glanceable Weather/Calendar/Music widgets with Wallet/Appearance/Photos/Camera, then Phone/Map/Music/Calendar/Reminders/Shopping/Food Delivery, then System Status/Quick Heart/Quick Disc. Appearance now lets users choose two through five visible pages. Reducing the count hides trailing retained pages only; templates, slot placements, and content survive unchanged, while Home edit mode always shows all five pages. Network, Stock, Assets, and the duplicate formal-page App Store entry remain available through App Store, Widget Center, Today View, or edit mode without appearing equally mature. Setup version 5 migrates only recognized earlier defaults, including the pre-Music release layout, while preserving customized layouts; focused unit and desktop/mobile Playwright cover migration, page count, route context, and App Store recovery.

98. Network & API now has an explicit Direct / Compatibility Proxy control. Direct remains the default; proxy disclosure explains credential routing and compatibility limits, while custom relay URL and optional proxy-access token use progressive disclosure. Desktop and Pixel 5 Chromium prove relay routing, hidden fake credentials, zero horizontal overflow, zero critical axe violations, and no page errors. The prepared relay code is not yet deployed.

99. Music's installed-app slice now includes the dedicated ChKSz follow-up, a no-key Radio Browser live-station preset, Music-owned direct/local intake, the first promoted Chat caller, and the active-journey Map media caller. `/music` keeps Discover, Albums, My Music, top-level Search, queue, and Now Playing ahead of configuration; Discover uses a stable weekly recommendation that stays independent from the current runtime track. Playing the recommendation loads it into the persistent in-app player, while recommendation, album, and song controls reflect their real play/pause state and Stop remains with current-player controls. The persistent in-app player appears only for a real runtime track. Now Playing's favorite action switches immediately to a strong accent solid-heart `Added` state, appears in My Music Favorites, and persists with public Music state. Its focused settings sheet presents `Add Music` as an IA peer of `Music Sources`, with URL/local-file modes separate from ChKSz, Radio Browser, custom JSON, and Gallery/image resources. Radio Browser appears as a focused live-radio choice with fixed HTTPS/MP3 filtering rather than exposing generic field mapping or a credential field. URL and imported songs use a compact management list rather than turning My Music into an asset intake surface. ChKSz results still resolve only at Play, NetEase lyrics stay in a focused listening sheet, and playlist-ID import remains a provider-owned My Music action. The Music floating player now has collapsed, expanded, and dismissed states; Map's traveling/paused-only panel stays above it and closing that panel leaves the float and playback intact. Desktop/simulated-mobile Playwright covers URL/local import and playback, no-key Radio Browser configuration/search/playback, Chat recipient/send/cancel/reload/quote/return, Map radio/float coexistence and return context, and overflow. Real-key rights/CORS smoke, true-device media behavior, Chat search, and external Map queue requests remain separate gates.

99A. The 2026-08-10 Music cache/favorite follow-up keeps the existing visual composition and adds a strong 42 px accent heart plus `Added / 已喜爱` label in Now Playing. Focused View coverage and all 12 desktop/mobile Music Playwright cases pass; manual default-desktop and 393 x 852 inspection confirms zero document/sheet horizontal overflow. This is simulated-browser evidence, not true-device acceptance.

99B. The 2026-08-10 Music Listen Now refinement gives the installed app a clearer record-shelf reading order without changing playback or provider behavior. Wide viewports pair the real weekly recommendation with a bounded three-track Recently Played overview and keep the album shelf as the next full-width layer; phone-sized layouts recompose that order as recommendation, albums, then recent playback. `See All` enters Library -> Recently Played. The 13-case focused Music View suite, full 226-file / 1646-test Vitest suite, lint, production build, governance and diff checks, all 12 `music-app.spec.js` desktop/simulated-Pixel-5 cases, and the 12-case default/zen visual gate pass. Browser inspection at 1280 x 800 and 393 x 852 confirms responsive composition, visible-copy cleanup, and zero document overflow. This remains simulated-browser evidence rather than true-device acceptance.

99C. The 2026-08-10 Music product-UI correction replaces the mixed `Listen Now / Browse / Library / Search` navigation with three user-goal spaces: `Discover / Albums / My Music`. Search moves to the top utility bar, while Queue and Music Settings remain utility actions. Discover now uses real cover art as an ambient full-width recommendation, four differentiated shortcut actions, an album shelf, and a bounded recent overview; Albums and My Music have separate visual identities rather than reusing the same page template. Desktop uses a dark record-library sidebar, while phone-sized layouts use a light three-item bottom bar with explicit selected state, readable inactive labels, and clearance from the device gesture indicator. This is the first Music implementation evaluated against the product-grade UI gate recorded in `docs/process/VISUAL_WORKFLOW.md`: visible hierarchy, consumer copy, purposeful page depth, icon-led controls, varied material, real imagery, distinct function-specific composition, interaction states, and responsive reconstruction are required together rather than deferred as later polish.

99D. `DONE (2026-08-10)`: the interrupted 99C validation was restored against `main` at `aee172c31af081c32f4e8a472aca55af0449dedf`, with the Music product-UI implementation from `75f7d9f` plus the later cache, floating-player, and Map-caller integrations present in the current tree. `npx.cmd vitest run tests/music-view.test.js tests/music-store.test.js tests/music-contract.test.js tests/chksz-music-adapter.test.js` passed 4 files / 47 tests; `npm.cmd run lint` passed; the first stable `npm.cmd run test` baseline passed 226 files / 1648 tests in 43.06s, while the final shared-tree rerun completed 227/228 files and 1670/1671 tests with the sole failure outside Music because concurrent Map work had raised `MAP_STORAGE_VERSION` from 2 to 3 without yet updating `tests/persistence-owner-inventory.test.js`; `npm.cmd run build` passed 390 modules in 12.88s with only the existing chunk-size warning; `npm.cmd run governance:check` passed 2 files / 13 tests; `npx.cmd playwright test e2e/music-map-journey.spec.js` passed desktop and simulated Pixel 5, 2/2 in 13.3s; `npm.cmd run test:e2e -- e2e/music-app.spec.js` passed desktop and simulated Pixel 5, 12/12 in 41.5s; `npm.cmd run test:visual` passed 12/12 in 35.4s; and `git diff --check` passed. The Music E2E now makes the configured custom-provider search deterministic, proves Discover, Albums, and My Music as distinct selected spaces, and waits for the system/Map owner mirrors before its ChKSz reload assertion without treating an unrelated Map reconciliation notice as Music recovery. Focused desktop/Pixel-5 evidence confirms top Search, the desktop record-library sidebar, the phone three-item bottom bar, inactive label color `rgb(119, 123, 128)`, no more than 80px bottom-navigation height, at least 4px label-to-gesture-indicator clearance, correct playback/Now Playing/favorite/floating-player states, and no document/body/Music/settings horizontal overflow beyond 1px. Manual screenshot review covered Discover, Albums, My Music, Now Playing, Settings, and the Home floating player at both viewport classes. Real-key rights/CORS, true-device media and safe-area behavior, Chat search, and external Map queue requests remain separate gates and were not claimed complete.

99E. `DONE (2026-08-11)`: Music now has a product-depth Album Detail slice instead of treating every album-card click as Play. Album card bodies open the cover-led detail without starting playback; a separate overlaid Play/Pause control reflects the real album state. Album Detail presents album metadata, synchronized album and track playback, favorite-all, queue confirmation, per-track Favorite/Playlist controls, and track-body entry into the existing Track Details surface. Closing Track Details returns to the album context. Wider layouts retain the record-library sidebar, while the 393 x 852 composition hides primary navigation as a focused drill-down and preserves the active in-app player with bottom clearance. The implementation reuses current cover art as restrained ambient media, removes the construction-facing `Opening details does not start playback` sentence, and introduces no new asset or playback contract. `tests/music-view.test.js` passes 1 file / 15 tests; `npm.cmd run lint` passes; `npm.cmd run test` passes 228 files / 1677 tests, with only the existing jsdom Canvas diagnostic on stderr; `npm.cmd run build` passes 393 modules with the existing chunk-size warning; `npm.cmd run governance:check` passes 2 files / 13 tests; `npm.cmd run test:e2e -- e2e/music-app.spec.js` passes desktop and simulated Pixel 5, 12/12; `npm.cmd run test:visual` passes 12/12; and `git diff --check` passes. Direct browser review at 1280 x 720 and 393 x 852 confirms separate inspect/play targets, synchronized Pause state, hidden mobile primary navigation, preserved in-app player, and zero document or Album Detail horizontal overflow. This simulated-browser evidence does not replace the existing true-device gate.

99F. `DONE (2026-08-11)`: Music now separates the browseable catalog, real playback history, and the user's collection. `My Music -> Songs` projects only saved tracks plus resolvable favorite and playlist members, so a new user sees zero personal songs while demo tracks remain available to Discover, Albums, Search, and playback. Playing a song updates Recently Played but does not silently save it. Discover and each My Music track category now use truthful empty states; the personal hero count and mosaic follow personal data. The hero's three glance tiles are read-only L0 statistics, leaving the category tabs as the only local L1 navigation. Focused Music coverage passes 4 files / 50 tests; lint passes; the serial full baseline passes 228 files / 1679 tests with only the existing jsdom Canvas diagnostic; production build passes 393 modules with the existing chunk-size warning; governance passes 2 files / 13 tests; Music Playwright passes 12/12 across desktop Chromium and simulated Pixel 5; the default/zen visual gate passes 12/12; and `git diff --check` passes. Roadmap status is unchanged, and this remains simulated-browser rather than true-device evidence.

99G. `DONE (2026-08-22)`: ChKSz search-result access diagnosis now follows progressive disclosure without changing the 99C product shell. Search rows show compact `VIP`, `Purchase`, or `Limited` hints only when bounded QQ/NetEase fields support them, while Play remains enabled because the hint is not final authorization truth. Playback failures distinguish premium, separate-purchase, restricted, missing-URL, Key, quota, rate-limit, provider-unavailable, and network cases; a successful one-step quality fallback appears as a short player status instead of changing the user's saved quality preference. Focused Music coverage passes 4 files / 61 tests; lint, the 303-file / 2186-test full suite, 594-module production build, governance at 2 files / 14 tests, all 12 desktop/simulated-Pixel-5 `music-app.spec.js` cases, the 16-case visual gate, and `git diff --check` pass. The ChKSz browser case proves the `VIP` label remains playable, `jymaster` falls back once to `standard`, and the successful actual quality is visible. Direct 1280 x 800 and 393 x 852 inspection confirms top Search, desktop sidebar, phone bottom bar, 7.17 px gesture-indicator clearance, and zero document/body horizontal overflow. Real membership authorization, true-device safe-area/media behavior, Chat search, and external Map queue requests remain separate gates.

99H. `DONE (2026-08-22)`: Music's non-Chat global player now defaults to a compact right-side capsule instead of occupying a large desktop/mobile surface. The collapsed capsule exposes only track identity, play/pause, expand, and dismiss; expansion reveals previous/next, quick tracks, journey radio, and explicit playback policy. Order mode advances automatically on track end and stops at the queue boundary, repeat-all wraps, repeat-one reloads the current track, and shuffle changes queue selection. The Music App's phone player no longer hides those controls: Now Playing provides the visible order/repeat-all/repeat-one/shuffle entry, while the main player and float expose accurate state names and accessibility state. `npx.cmd vitest run tests/music-view.test.js tests/music-store.test.js` passes 2 files / 40 tests; `npm.cmd run lint` passes; `npm.cmd run test` passes 303 files / 2187 tests with only the existing jsdom Audio/Canvas diagnostics; `npm.cmd run build` passes 594 modules with only the existing chunk-size warning; `npm.cmd run governance:check` passes 2 files / 14 tests; `npm.cmd run test:e2e -- e2e/music-app.spec.js e2e/music-map-journey.spec.js` passes 14/14 across desktop Chromium and simulated Pixel 5; `npm.cmd run test:visual` passes 16/16; and `git diff --check` passes. Direct 393 x 852 inspection confirms a 252 px collapsed capsule, about 16.5 px clearance from the Home Dock, a 369 px expanded surface, successful order/repeat-all/repeat-one switching, a visible phone Now Playing mode entry, and zero horizontal overflow in either state. Desktop/simulated-Pixel-5 Playwright confirms readable labels, correct playback/control state, float/Map layer order, and bottom/gesture clearance. Initial playback without a user gesture remains browser-controlled; real-key provider rights/CORS, true-device media and safe-area behavior, Chat search, and external Map queue requests remain separate gates.

100. Widget Center's release hardening now applies one appearance-code contract to direct create/update, JSON import, persisted legacy hydration, market previews, and Home rendering. Active elements, navigation attributes, inline handlers, and dangerous CSS are rejected on write; legacy definitions are retained with unsafe markup removed; every preview is scriptless and CSP-restricted, while click actions remain SchatPhone metadata. Official style copies keep stable preset identity across locale changes and receive numbered copy names. Mobile Custom/Import sheets and style previews now own focus entry, Tab containment, Escape close, background inertness, focus restoration, and in-sheet validation feedback. Focused Vitest plus desktop and simulated Pixel 5 Playwright cover unsafe drafts, invalid/valid import, preview focus, Home edit handoff, persistence, overflow, and default/zen visual states. Named true-device keyboard, browser-chrome, and safe-area proof remains part of the release matrix.

101. Visual governance now records the user's repeated quality feedback as a product-grade first-implementation contract rather than leaving it to later polish. The contract rejects construction-narration UX, flat all-in-one pages, excessive text buttons, unconsidered palette/material/background/media/motion, label-swapped template reuse, and silent omission of imagery. It requires L0-L3/container planning, state coverage, icon/control review, on-demand reference discovery, proactive asset or image-generation decisions, and product-grade mobile/wide acceptance evidence. This changes the quality gate for future visual slices without promoting a page-local visual implementation or changing roadmap priority.

102. Visual governance now treats a vague feature wish, feeling, concern, or reference as valid intake instead of requiring the user to supply a formal brief or choose tools. The assistant must infer the surface's product role, experience promise, direct content/media presentation, page depth, state needs, and justified expressive complexity from repository evidence. Bounded UI work defaults to the shortest product-complete implementation path; Product Design direction exploration and disposable experience prototypes are optional escalation paths only when identity, hierarchy, gesture, scene, motion, sound, haptics, or another consequential experience choice is genuinely uncertain. A liked example remains a clue rather than a template or global standard. This workflow refinement changes no active roadmap priority and promotes no page-local implementation.
103. `DONE (2026-08-13)`: Phone now completes its first product-level local call flow. A three-space Recents / Contacts / Keypad navigation makes placing a call a primary task; number entry, contact matching, direct contact calls, history redial, calling/connected state, elapsed duration, mute, speaker, keypad tones, and hang-up are implemented in the existing Phone view. Connected calls persist through the existing Phone Store and relationship-fact Adapter, while calls ended before connection persist as failed outgoing records without relationship impact. The existing manual record sheet remains available as a secondary repair/intake path. No carrier, VoIP, microphone, or remote communication claim is made. Focused Vitest passes 7/7, Phone Playwright passes 4/4 across desktop/mobile and default/zen themes with critical Axe and overflow checks, lint/build/diff checks pass, and direct in-app-browser inspection at 422 x 698 confirms zero horizontal overflow plus full first-viewport access to keypad, call controls, hang-up, contacts, and bottom navigation. True-device and real-time audio remain outside this local simulation boundary. Roadmap priority is unchanged.
104. `DONE (2026-08-15)`: Weather Terrarium now has a real-data product loop without adding a standalone Home icon. Open-Meteo supplies normalized current, hourly, and seven-day forecasts; `settings.weather` owns saved cities, active city, and the default-open / expand / no-action widget behavior, while a separate 30-minute local cache is rebuildable and excluded from backup. `/weather` provides a current-weather scene, forecasts, detail metrics, attribution, offline/stale/error handling, and city search. Home and Widget Center use the same active forecast, and exact WMO conditions map into the accepted Terrarium's clear/cloudy/rain/night states. Focused Vitest passes 71/71, targeted Playwright passes desktop and simulated Pixel 5 at 2/2, production build and lint pass, and live `393x852` review fetched Tokyo and Shanghai data, verified city switching and rain-state Home synchronization, found and fixed the global-status-bar hit-area collision, and confirmed zero document horizontal overflow. Four-state subjective visual acceptance and named true-device browser-chrome/offline/safe-area evidence remain open.
105. `DONE (2026-08-15)`: Weather now separates the world-facing display location from the private real-city data source. Existing active-city preferences migrate into a global mapping; `settings.weather.mapping` keeps one global default plus future-safe per-world overrides keyed by the canonical WorldBook world ID, never by World Pack ID. Weather Settings presents a final preview, all-world/current-world scope, follow-Map/custom/source display modes, real-city search, and an explicit source-disclosure switch that is off by default. Home and Widget Center render the mapped display name and relative hourly labels, while Chat receives a dynamic weather projection containing only the mapped name and normalized conditions unless the user explicitly permits source-city disclosure. Desktop and simulated Pixel 5 Playwright prove `首尔` displayed from Shanghai weather through Weather and Home with no horizontal overflow; focused weather, Chat-context, and Widgets tests pass 37/37. On 2026-08-16 the focused Weather flow also gained an explicit reduced-motion assertion for the Terrarium's stage, aura, clouds, atmosphere, rain, mist, stars, and glint, and a single comparison board was prepared for clear/cloudy/rain/night plus Widget Center and Home collapsed/expanded review. Four-state subjective visual acceptance and named true-device checks remain open.
106. `DONE (2026-08-16)`: First UI sound-effects pilot landed on the already-installed `uisfx@0.4.0` dependency. A lazy, environment-safe service (`src/lib/ui-sfx.js`) wraps one `soft`-pack player at volume 0.5, unlocked by the first pointer/keyboard gesture from `App.vue`; playback is best-effort and silent on failure. `settings.appearance.soundEffectsEnabled` mirrors the haptic feedback contract end to end: default on, normalized on load and import, exported/imported through appearance packs, and toggled in Appearance Settings beside haptic feedback. Pilot cues: foreground `notification` on the central `addNotification` chokepoint (1.5 s cooldown; background keeps using real system push), `send` on chat text send, `open` on Home app/folder entry, and semantic `success`/`warning` cues on Home layout-outcome toasts while purely instructional toasts stay silent. Focused Vitest passes 9/9 for the new service and pack-field tests, `chat-appearance` Playwright passes 2/2, lint and production build pass. Full-suite Vitest shows 8 failures in persistence/storage-diagnostic/widget-import files verified pre-existing in the dirty worktree (they pass at HEAD) and unrelated to this slice. Real-speaker listening acceptance, per-cue pack choice, and any further cue surfaces remain open. Roadmap priority is unchanged.

### 2026-07-16 Visual Portfolio Audit

This matrix is a current-state classification, not an execution board. Active priority remains in `docs/roadmap/TODO_ROADMAP.md`, and only one focused visual scope should be promoted at a time.

| Portfolio | Current maturity | Evidence-backed gap | Complete target | Treatment |
| --- | --- | --- | --- | --- |
| Device shell, Lock, Home, notifications | Usable system baseline; Home has a strong mobile identity | Wide desktop viewports stretch the phone experience into sparse full-width pages; Lock and notification states have narrower visual evidence than Home | A believable device frame and spatial shell at mobile and wide viewports, with stable safe areas, return context, notification materials, and day/night parity | Decide the wide-viewport shell contract first; then preserve Home and apply focused shell polish |
| Settings, Appearance, Widgets, App Store, Network, Profile | Usable native-system baseline with progressive-disclosure work already landed | Visual automation covers only Home, Settings, and Appearance; several deep forms still mix system tokens with raw utility colors and page-local card patterns | One calm system-control language with semantic tokens, scan-first primary states, focused execution sheets, and explicit save/feedback | Targeted token and state cleanup; no full restart |
| Chat, Chat Directory, Chat Me, Chat Settings, Chat Appearance, Groups | Strong mobile app identity and coherent ownership split | Wide layouts are sparse, and supporting pages vary in density and finish | A warm, conversation-first installed app that remains phone-like on wide screens without becoming a stretched web inbox | Preserve identity; polish adaptive layout and state consistency |
| Contacts, WorldBook, Book | Functionally mature IA with several phone-sized flows | Contacts and WorldBook remain very dense; WorldBook is still visually card-heavy; true-device touch, keyboard, and safe-area checks are incomplete | A readable role archive and in-phone knowledge system with clear overview-to-detail progression and soft review states | Target one state at a time; do not rebuild completed ownership loops |
| Map, Calendar, Reminders | Map has a strong immersive identity; Calendar and Reminders have usable functional baselines | Schedule surfaces still read as generic status cards rather than a confident time-management app | Preserve Map; give Calendar/Reminders a schedule-first identity while keeping source attribution and confirmation boundaries | Map polish only; Calendar/Reminders are targeted rebuild candidates |
| Camera, Gallery, and Assets | Camera has a focused first capture/settings identity; Gallery is a functional asset manager; Assets has a stronger domain presentation | Gallery still lacks the People smart view and leads empty state with management controls; Camera lacks true-device evidence | A real capture-to-review-to-Photos loop with visual collections first and cross-module asset management disclosed second | Preserve Camera's hierarchy; promote Gallery People and Photos-first work separately |
| Music | focused installed app with real browser playback, library, queue, direct URL/local-file intake, generic JSON, no-key Radio Browser live radio, ChKSz source setup, on-demand resolution, NetEase lyrics/playlist intake, global floating player, first Chat track-share caller, and active-journey Map music/radio caller | live-station uptime is external; real-key rights/CORS and true-device media evidence are not complete; local-binary backup/relink, Chat search, and external Map queue requests remain contract seams | A believable personal listening app whose source and file complexity stays behind consumer playback surfaces | Preserve the identity; promote real-provider evidence or another caller-specific slice only from the Music contract |
| Food Delivery and Shopping | Food Delivery has the strongest asset-led commerce presentation; Shopping has a functional shared shell | Food Delivery still needs detail/checkout/responsive polish; Shopping remains generic and does not yet express selected platform identity strongly | Distinct consumer apps with source-owned browsing/cart/order truth and clear mini-app/platform identities | Preserve Food Delivery direction; target Shopping identity separately |
| Phone | Focused local-call app with Recents, Contacts, Keypad, and a simulated active-call loop | True-device touch/safe-area evidence and any future audio/VoIP contract remain absent | A believable in-device Phone surface whose local simulation boundary stays explicit | Preserve the current hierarchy; validate true-device behavior before deeper media or communication scope |
| Wallet, Stock, Files | Functional MVPs with generic form/card presentation | Product roles are not mature enough to justify three independent visual systems | Clean secondary apps that inherit shell ergonomics until deeper product meaning is promoted | Hold bespoke rebuilds; apply only shared-shell and accessibility fixes |
| World Pack hybrid paths | First cross-entry and target-app context seams are implemented | True-device validation and broader label/accent/safe-default hardening remain incomplete | Parent app keeps visual ownership while World Pack supplies restrained context, terminology, accent, and safe defaults | Validate the existing loop before adding another archetype or visual variant |
| Visual validation | Single Playwright path is healthy; the 2026-07-16 audit rendered 29 mobile route surfaces and 8 desktop representatives without page errors | Automated visual gate still covers only Home, Settings, and Appearance; screenshots are evidence, not pixel baselines | Risk-based structural, overflow, accessibility, and screenshot evidence through one toolchain | Extend only for repeated regression risk; do not add a parallel visual platform |

Complete portfolio target:

1. preserve a believable phone frame and interaction scale at every supported viewport;
2. keep Native System surfaces coherent through shared semantic tokens, chrome, spacing, motion, and state patterns;
3. let installed apps use distinct identities without abandoning safe areas, navigation, accessibility, or touch ergonomics;
4. make entry and parent context decide hybrid-surface presentation;
5. lead content-rich apps with real content and imagery instead of management forms;
6. keep normal, empty, loading, error, edit, selected, success, and destructive states visually intentional;
7. use the existing Playwright path plus targeted true-device checks as evidence, without making visual work a second roadmap.

Still incomplete:

1. the wide-viewport shell contract, Lock/notification state coverage, Gallery's Photos-first entry, Calendar/Reminders schedule identity, and Shopping platform identity still need focused visual work; Chat, Map, Home, and Food Delivery now have directions to preserve, with later work limited to adaptive layout, missing states, detail/checkout ergonomics, responsive density, and observed inconsistencies;
2. some pages still mix destructive actions and ordinary edits too closely;
3. Contacts detail and memory 4.2 are complete at current acceptance; remaining Contacts work is later polish or an explicitly promoted template-adaptation visual diff.
4. Home layout storage still keeps ordered page arrays as a compatibility/recovery layer. A later slice should add per-instance action overrides if users need the same widget definition to behave differently in different slots.
5. World Pack app-entry unlocking now has a first implementation; the world UX package has first target-app context treatment in Shopping, Food Delivery, Calendar, and Map, the Optional capability Packs surface hands off to App Store's `World` section instead of launching entries from Settings, App Store now explains that handoff before opening, and the nonstandard-app review UI is landed with loading/empty/error/rejection handling. Desktop and simulated-phone Playwright now protect activation, placement, persistence, four-target launch/return, disabled fallback, long labels, overflow, page errors, and critical axe, but true-device touch feel, browser chrome, keyboard, and safe-area checks remain incomplete.
6. Custom CSS ownership is now split by user meaning: Appearance keeps global CSS, Chat keeps Chat-scoped CSS, App Store owns app icon identity and standard app skins, and persisted app/world-app scoped CSS stays compatibility/runtime state until a later app/World Pack-owned authoring surface is explicitly designed.
7. Contacts phone-like entry and the WorldBook -> Contacts value-flow both have simulated mobile coverage, but still need true-device checks for touch feel, browser chrome, keyboard behavior, and safe-area spacing.
8. Mini Scene Settings, transform-profile editing/preview, Text Presenter, and sandboxed HTML Presenter are future roadmap 4.8 stages. Their visual work cannot begin before the corresponding shared Module and security stage is promoted.
9. Food Delivery still needs formal Food Platform order-state artwork and true-device evidence. Peach Cloud's product-image identity review is complete, but that completion does not authorize another visual slice or advance the ordinary cross-module consequence roadmap task.

## 2. Recommended Next Slice

The first successful Chat activation loop, explicit custom role -> Chat journey, ordinary Shopping consequence, Wallet quote detail, configurable multi-screen Home release curation, and release-local complete v3 backup/recovery boundary are complete. Vercel/Cloudflare root infrastructure is deployed, and the restricted dynamic-relay UI/code is locally validated but still unpushed. No additional page-local visual slice is active while deployed relay, PWA, and true-device product proof remain the next P1 release gate.

1. preserve the curated multi-screen Home defaults and page-layout independence while proving deployed PWA/install/relaunch behavior, hosted-provider Chat, and named true-device backup recovery;
2. fold World Pack, Chat, and Home touch/safe-area checks into the named true-device release matrix rather than treating another archetype as progress;
3. fix only observed release-surface problems; do not refill Home merely because a module exists;
4. defer Gallery Photos-first, wide-viewport shell redesign, app-by-app visual consistency, and Mini Scene Presenter work until the roadmap 4.9 gates are complete.
5. keep Music follow-up limited to real-key/CORS and true-device evidence or one remaining separately promoted caller; preserve the accepted Map-panel/global-float split and do not turn the focused ChKSz/Radio Browser/custom-source sheet into a global Network dashboard.

## 3. Do Not Do

1. Do not use visual polish to hide unresolved IA confusion.
2. Do not overbuild a decorative system before the module workflow is stable.
3. Do not make destructive actions look equivalent to safe edits.
4. Do not make World Pack UI/UX changes visible only in Settings when the user is meant to experience them globally.
5. Do not make Book/WorldBook activation look like Mini Scene popup permission, and do not let interactive content imitate trusted system controls outside the sandbox.

## 4. Must Sync When Working Here

At the end of a meaningful round, check and update:

1. `README.md`
2. this file
3. `PRODUCT_BOUNDARY.md`
4. `IMPLEMENTATION_WORKSTREAMS.md`
5. `docs/process/VISUAL_WORKFLOW.md`
6. `docs/overview/APPEARANCE_REBUILD_SCOPE.md`
7. `docs/overview/VISUAL_STYLE_DIRECTION_BRIEF.md`
8. `docs/product-decisions/HOME_DESKTOP_LAYOUT_SYSTEM.md`
9. `docs/references/VISUAL_ASSET_LIBRARY.md`
10. `docs/architecture/MINI_SCENE_MODULE_CONTRACT.md` when Mini Scene Settings, Book profile editor, or presenter meaning changes
11. `docs/architecture/MUSIC_MODULE_CONTRACT.md` when Music provider, playback, persistence, floating-player, or Chat/Map presentation meaning changes
107. `DONE (2026-08-19)`: SchatPhone now has a real incoming-call experience with a built-in ringtone library. Six call ringtones are synthesized entirely in-repo by `scripts/build-ringtones.mjs` (Gran Vals public-domain excerpt rendered with original Karplus-Strong synthesis plus five original melodies: marimba, soft piano, electromechanical bell, feature-phone square-wave, and glockenspiel chime; 32 kHz mono WAV in `public/audio/ringtones/` with a SOURCES.md that claims no third-party audio). `src/lib/ringtone.js` owns the manifest plus loop-capable HTMLAudio playback that stops prior ringtones before starting a new one. `settings.appearance.ringtoneId`/`ringtoneEnabled` follow the sound-effects contract end to end (defaults, load/import normalization, appearance-pack export, and a SettingsSoundSubpage picker with preview). The Phone Store gains an incoming-call state machine (`receiveIncomingCall`/`acceptIncomingCall`/`consumeAcceptedIncomingCall`/`declineIncomingCall` plus a 25 s missed timeout that writes a missed record and fires the existing missed-call notification and Calendar cue); incoming ringing state is intentionally not persisted. A global `IncomingCallOverlay` in `App.vue` rings above every surface including the lock screen, loops the selected ringtone, answers from lock by unlocking and routing to Phone, and declines in place; PhoneView consumes the accepted call into the existing connected-call UI (timer, mute, speaker, keypad) and saves direction `incoming` records, and the Contacts tab gains an explicit per-contact "simulate incoming call" action as the v1 owner-native trigger. Focused Vitest passes 7/7, the full suite passes 294 files / 2081 tests, `phone-incoming-call` Playwright passes 4/4 across desktop and simulated mobile, existing phone/contacts Playwright suites pass, lint, production build, and diff checks pass. Organic autonomous incoming calls (a simulation event family under `event-runtime-and-world-hub`), real-speaker listening acceptance, and resolving the licensing risk of the earlier zedge-sourced notification mp3s remain open follow-ups. Roadmap priority is unchanged.
108. `DCF-03 DONE (2026-08-21)`: the ringtone preview correction already integrated at `42742e5` is protected by regression test `e9607c0`. Settings preview remains available when unrelated system sound effects are off, becomes unavailable when ringtones are off, and continues to share `ringtoneEnabled` with real incoming calls. This governance closure changes no ringtone media, phone call-audio profile, keypad tones, Calendar behavior, or visual layout.
109. `DONE (2026-08-22)`: Vibration is now a first-class Sounds & Haptics citizen instead of a Home-only experiment. A new `src/lib/haptics.js` centralizes Web Vibration behind named patterns (`tap`/`light`/`medium`/`heavy`/`success`/`warning`/`message`/`ring`), an enabled gate, and best-effort guards; `startRingVibration`/`stopRingVibration` loop the ring pattern while a call rings and cancel cleanly only when a loop actually ran (unsupported browsers and iOS Safari, which ships no Web Vibration API, stay silent no-ops with honest copy). Wiring: incoming-call vibration runs independently of the ringtone mute switch (silent-mode calls still vibrate), foreground notification arrival vibrates with the message pattern under `hapticFeedbackEnabled` (independent of `soundEffectsEnabled`), chat text send gives a light pulse, and HomeView's existing 29 `maybeVibrate` sites now delegate to the shared service. Settings IA now matches phone-system convention: the Sounds entry is renamed 声音与触感 (Sounds & Haptics) and its subpage gains a Haptics card (toggle plus a support-aware Test Vibration button) beside global sound effects, ringtone, and call audio; the duplicate haptic toggle was removed from the Appearance app so one setting has exactly one home, with no settings-key, appearance-pack, or Chat override contract change. Focused Vitest passes 5/5 for haptics plus 62 targeted tests; `phone-audio-settings` Playwright passes with the new haptics-placement and Appearance-removal assertions across desktop and simulated mobile; settings-landing-quality and phone-incoming-call suites pass; scoped lint on touched files and production build pass (the repo-wide lint failure and two full-suite runs' scattered failures were verified to come from an unrelated untracked `output/imagegen` artifact and localStorage-quota flakiness under parallel load, respectively — the flagged files pass 111/111 in isolation). True-device Android vibration feel, a per-surface vibration strength option, and iOS haptic alternatives remain open. Roadmap priority is unchanged.
110. `DCF-04 DONE (2026-08-22)` at `26534bd`: the global incoming-call dialog now receives keyboard focus when it opens, keeps forward and reverse Tab movement inside Decline/Accept, and restores the connected trigger after decline. The existing ringtone, vibration, call state, and visual composition are unchanged. Focused Playwright passes 4/4 across desktop Chromium and simulated mobile.
111. `SHP-1 DONE (2026-08-23)`: roadmap 4.16's app-shell preview wave lands its first S1 fixture shell, Daon Mail (다온메일), as the reusable pattern proof for the `EVENT_APP_SHELL_PRIORITY_MATRIX.md` waves. The shell uses a fictional Korean portal-mail identity — portal-green header with wordmark, desktop three-pane layout (folder rail with account card and unread badges, dense row list with avatar chips/label chips/attachment marks, reading pane), a mobile folder drawer, a compose surface with local drafts and local-sent, search over senders/subjects/bodies, and complete empty/no-result states — over fixture data in `src/lib/mail-shell-data.js` (one account, six folders, nine bilingual inbox threads plus one spam fixture referencing the existing Seoul world). Device-local preview state (`schatphone:mail-shell:preview-state`, registered in the persistence owner inventory at v10 and excluded from backup) covers read/star/archive/drafts/local-sent with an S2-swappable composable seam (`useMailShellState`). Wiring adds the `/mail` route, the `app_daon_mail` Home tile on formal page 2 via a new `layout-h` 12-slot template as that page's default (the legacy `app_mail` Network alias is preserved untouched and existing explicit template choices are unchanged, with `HOME_DESKTOP_SETUP_VERSION` bumped to 7 and the prior default captured for pristine-layout migration), icon customization registration, and a `mail` return-source entry so the hospital invitation card's Calendar deep link round-trips. Focused Vitest passes 15 mail tests plus updated layout/appearance/import expectations; the full suite passes 304 files / 2202 tests; lint, production build, `governance:check`, and `git diff --check` pass; the 6-case mail Playwright suite and the 20-case visual gate (day/zen × desktop/simulated Pixel 5) pass with screenshots under `output/e2e/mail-app-shell/`. This is an S1 fixture preview, not an S2 owner: no production Store, backup section, real delivery, notification write, or Event Runtime participation exists or is implied. Roadmap 4.16 SHP-1 is marked done in the live roadmap; Healthcare/Housing/Community shells wait for separate user promotion.
112. `SHP-1B DONE (2026-08-23)`: Daon Mail gains explicit-entry AI arrival. A header `Receive` action makes exactly one provider call per click through the shared `callAI` transport (the user's Network & API settings, direct or proxy) with bounded prompt context — user-managed sender whitelist, active world name plus a ~320-character worldview excerpt, Self Profile display name and public role, and the current system language — and commits one validated unread letter with provider/model provenance into preview state v2 (`received[]`, capped at 60). `src/lib/mail-shell-arrival.js` owns the prompt, strict JSON validation (sender shape, length caps, label allowlist, HTML rejection), and fail-closed error codes; missing providers surface honest recovery into Network & API and invalid drafts never fall back to a deterministic substitute. The sender whitelist is user configuration (`schatphone:mail-shell:sender-whitelist`, inventory v11, excluded from backup) defaulting to fixture institutions, manageable in-app through `MailSettingsSheet.vue` (add/remove/restore, `allowNewSenders` toggle), with validated AI-invented senders enrolled under an explicit `generated` origin — the product direction the user set for configurable world growth. Ordinary browsing stays zero-token; this receive action is the shell's only provider entry and is recorded in roadmap 4.16 as the sole S1 zero-provider exception. Focused coverage passes 38 mail tests (15 view + 19 arrival/whitelist + inventory) including fail-closed, enrollment, and honest-recovery paths; the 8-case mail Playwright suite adds a real fake-provider arrival round trip across desktop and simulated Pixel 5 with model provenance and reload persistence. Arrival system notifications are explicitly recorded as pending the Notification Center shell in `MAIL_SHELL_FEATURE_PLAN.md`, together with world-pack mailbox backfill and the S2 owner seam. Roadmap 4.16 records `SHP-1B MAIL_AI_ARRIVAL_DONE 2026-08-23`.
113. `DONE (2026-08-23)`: global Phone ringtone Settings now complete the previously selected familiar-device set instead of exposing only the Samsung additions. Four separate ringtone IDs and community-source assets add Nokia Tune, iPhone Marimba, a Western Electric-style Old Phone Ring, and Sony Ericsson Morning Glow alongside the six project-original ringtones and four Samsung choices. The Marimba asset removes 0.967 seconds of leading silent MP3 frames; the selected Old Phone Ring is already a loop-suitable 6.6-second excerpt. Asset provenance is recorded in `public/audio/brand/SOURCES.md`; notification sounds, call-state audio, and Chat-owned sound settings remain unchanged and semantically separate. Focused Vitest passes 21/21; `phone-audio-settings` Playwright passes 4/4 across desktop and simulated mobile with asset fetch/decode plus selection persistence; scoped lint, production build, governance, and diff checks pass. Repo-wide lint remains blocked by the unrelated `src/lib/navigation-return.js` `no-control-regex` error, while the full suite passes 305/307 files and 2251/2253 tests with two unrelated dirty-tree failures in the Home layout setup-version assertion and Browser/Community persistence inventory. Roadmap priority is unchanged.
114. `SHP-2 DONE (2026-08-23)`: roadmap 4.16's user-promoted shell wave now includes four additional S1 products with distinct installed-app identities and ordinary no-event loops. `Prism Browser / 折光浏览器` provides zero-provider Help and public Current World search, honest unavailable Web handling, source-filtered results, detail, history, bookmarks, and return context. `Ripple / 涟漪` provides Following/Explore/News/Saved, account panels, committed post detail, and visible verified/account-claim/published-content semantics without becoming a Community/Media owner. `Ondam Care / 温谈健康` provides institution/service discovery, a first-screen appointment/report overview, local appointment create/reschedule/cancel, authored report read/correction handling, and stable Map place handoff without diagnosis or real-health intake. `Jari / 住处` provides rent/buy discovery, filters, favorites/recent, listing detail, local viewing drafts, and a real Map neighborhood handoff; its residential references come from the existing Map place catalog, and neither the shell nor its abstract layout media create a fake map or exact listing coordinate. Shared routes, Home/App Store entries, appearance registration, return context, version-9 pristine-default Home migration, and four excluded preview-state inventory entries are integrated while customized Home layouts remain untouched. The combined focused slice passes 9 files / 93 tests; the four Playwright specs pass 30/30 across desktop and simulated Pixel 5; the 20-case visual gate, lint, production build (653 modules), governance, and diff checks pass. The full Vitest run reaches 310/312 files and 2302/2307 tests; its four Food Delivery failures come from concurrent dirty Food Delivery/Wallet work outside this slice, while the persistence inventory timeout passes 9/9 in isolation. The shells remain S1: no production Store, backup section, notification write, cross-owner mutation, provider call (outside Mail's separately accepted explicit Receive action), or Event Runtime integration is implied.
115. `SYSTEM APP ICON THEMES V1 DONE (2026-08-23)`: Appearance now owns a whole-pack icon style for a curated native-system App allowlist independently from day/night color theme. `src/lib/system-app-icon-theme.js` defines the allowlist and the `classic` / `soft-rounded` Font Awesome mappings, while the existing `resolveAppIconMeta` pipeline enforces `explicit per-app override > selected system App pack > built-in default`. Home, Dock, App Store, and notification presentation consume the shared resolution; Chat, Back, Search, Close, status-bar glyphs, shopping/food mini-app brand images, and branded Mail/Browser/Community/Healthcare/Housing entries remain outside the pack. The persisted `settings.appearance.systemAppIconTheme` field is included in portable global appearance packs and accepts the mistaken uncommitted V1 `systemIconTheme` key as migration input. The dedicated Appearance page previews Contacts, Settings, Widgets, and Phone and explains the independent-App boundary. Focused Vitest passes 27/27; the full suite reaches 312/313 files and 2316/2317 tests with only the existing persistence-inventory timeout, which passes 9/9 in isolation. Lint, production build (654 modules), the dedicated desktop/simulated-Pixel-5 Playwright flow (2/2), the visual gate (20/20), and governance pass. Roadmap priority is unchanged; future generated or authored packs can extend this contract without weakening App Store identity ownership.
116. `SHP-2 APP LOGOS ACCEPTED (2026-08-23)`: the four S1 shell entries now use user-approved PNG identities through the shared app-icon resolver: Prism Browser uses the refracted A monogram, Ripple keeps its first-round ripple mark, Ondam Care uses the calm-palette A1 baby-elephant doctor, and Jari uses the blue-yellow C residence mark. Home and App Store resolve the same defaults, while an explicit App Store per-app icon override still wins and the previous Font Awesome glyphs remain accessible fallbacks. The selected runtime bytes are published under `images/ui-assets/shared/app-icons/`; all non-selected animal-doctor candidates are retained as protected project sources for possible later mascots or other apps rather than deleted. This is visual identity completion only: S1/S2/S3 product boundaries and roadmap priority do not change.
117. `SYSTEM APP ICON ART HANDOFF (2026-08-23)`: the system-App icon switching logic is accepted and frozen at V1. The next slice is visual asset direction only: design or generate coherent candidate packs for the existing curated native-system allowlist, review them at Home/Dock sizes in day and night themes, and replace or extend pack metadata only after the user selects a direction. Do not reopen resolver priority (`per-app override > selected system pack > built-in default`), add commercial/branded apps to the allowlist, or include Back/Search/Close/status-bar glyphs. `ip-as-logo` may support an optional character-led pack exploration, but it is not required and must follow its own three-direction/six-candidate approval flow before generation. No icon-art round is active yet.
118. `APPEARANCE FOUR-LAYER FOUNDATION DONE (2026-08-24)`: Appearance now separates day/night mode, system-theme tokens/materials, curated native-system App icon packs, and one-click style-kit composition. `src/lib/system-appearance-theme.js` owns the registries and normalization contract; existing `currentTheme=default/zen` remains a compatibility bridge for saves, CSS, backups, and route tests. The first style kit is System Classic and proves the composition behavior without shipping unfinished themed art: it applies system theme + icon pack, changes wallpaper only when explicitly selected, and reports a customized derivative after a composed part diverges. Style kits never mutate Home layout/widgets, Chat appearance, app skins, global custom CSS, commercial/branded logos, or explicit per-app icon overrides. Portable appearance packs advance to schema V2 with legacy theme-only import migration. Focused Vitest covers the four axes, legacy restoration, personal-wallpaper preservation, explicit kit wallpaper application, Appearance controls, and pack migration. Dedicated Playwright coverage passes 4/4 across desktop Chromium and simulated Pixel 5, including personal-wallpaper retention, persistence after reload, commercial-logo preservation, page-error checks, and horizontal-overflow checks; the full Vitest suite passes 314 files / 2326 tests, with lint, the 655-module production build, governance, and diff checks also passing. The next safe visual slice is Cloud Pastel day/night token and asset authorship on top of this frozen foundation, followed separately by Dessert Bakery and Misty Glass.
119. `CHAT ICON OWNERSHIP SPLIT (2026-08-24)`: Chat is an independent installed App rather than a native-system App icon-theme target. Switching a system App icon pack must leave Chat's built-in or explicit per-App identity unchanged. This does not alter Chat routes, Dock/Home placement, App Store customization, Chat-scoped appearance, messages, or runtime ownership.
120. `APP STORE IDENTITY COVERAGE CLOSED (2026-08-24)`: every real independent App listed by App Store now has a durable per-App icon/display-name path. Agenda Journey gains shared icon metadata and persistence support, Files becomes a formal App Store entry, and the identity action is gated by the actual customization registry instead of appearing for unsupported entries. Chat keeps this independent capability after leaving the system App icon-pack allowlist. Source-backed folder mini apps retain their entry-presentation path; dynamic World Pack entries and the non-App `More` aggregator remain excluded.
121. `CLOUD PASTEL ANIMAL ICON MASTERS 6/20 ACCEPTED (2026-08-24)`: the user approved the first six `ip-as-logo` visual masters for an all-animal Cloud Pastel system App icon direction: Phone lop rabbit V4, Camera owl V2, Weather cloud sheep V5, Calendar bear V2, Map turtle V4, and Settings gear beetle V2. Byte-identical stable copies, source-version mapping, and SHA-256 hashes are frozen under `output/imagegen/cloud-pastel-animal-icons-accepted-v1/`. This is approved visual authorship for 6 of the 20 existing native-system icon-theme targets, not a complete pack and not runtime wiring. The remaining 14 targets require a separately approved asset round; Chat, commercial/branded Apps, explicit per-App overrides, and the separate object-personification direction retain their established boundaries.
122. `SYSTEM NOTIFICATION CENTER S1 DONE (2026-08-24)`: the selected App-grouped visual direction now ships as an unlocked native-system shade entered by status-bar tap or downward gesture, with no Home/App Store icon and no route. The production surface adapts the concept into real multi-notification App groups, All/Unread filters, read/dismiss/clear operations, explicit close, owner deep links, empty/disabled states, focus containment/restoration, reduced motion, long-text wrapping, and shared App icon/theme resolution. `design-qa.md` records the source-to-implementation comparison and passes typography, spacing, system-token color/material, live icon fidelity, and bilingual copy review. Dedicated Playwright passes 8/8 across desktop Chromium and simulated Pixel 5 with day/night, zh/en, accessibility, owner-record non-mutation, and zero-horizontal-overflow checks; no physical-device evidence is claimed.
123. `CLOUD PASTEL ANIMAL ICON PACK 6/20 + STYLE KIT INTEGRATED (2026-08-24)`: Appearance exposes the user-approved `cloud-pastel-animals` pack as `云朵动物 / Cloud Animals`. Phone, Camera, Weather, Calendar, Map, and Settings resolve the six accepted `ip-as-logo` WebP masters through the shared Home, Dock, App Store, and notification icon pipeline; the other 14 native-system targets deliberately retain their classic fallback until a later approved asset round. Explicit App Store per-App overrides still win, while Chat and commercial/branded Apps remain outside the system pack. Cloud Pastel is now also registered as a real one-click style kit that composes the `cloud-pastel` interface theme with `cloud-pastel-animals` and optionally its day/night recommended wallpaper; users no longer need a second trip to the icon page. Style Kits lead the Theme page, while day/night and interface-theme controls are explicitly presented as independent customization. Focused foundation/icon Vitest passes 29/29 and the dedicated Playwright flow passes 8/8 across desktop Chromium and simulated Pixel 5, including one-click Home icon proof, persistence, classic fallback, Chat isolation, personal-wallpaper preservation, page-error, and horizontal-overflow checks. The UI reports `Kit Applied` rather than claiming the still-partial 6/20 icon coverage is complete. The separate object-personification direction remains unselected; roadmap priority is unchanged.
124. `SHP-4 ORGANIZATION WORKPLACE S1 DONE (2026-08-24)`: `工作台 / Work Hub` establishes a product-specific workplace hierarchy rather than an event card or generic office dashboard; the earlier `幕间 / Interlude` name was retired because it did not communicate daily work utility clearly enough. The artist-first shell keeps the accepted deep-navy, paper-cream, and coral visual language across Today, Channels, Work, and Org, with call-sheet/backstage execution surfaces and a company credential. Org adds one progressive-disclosure naming editor instead of redirecting users to App Store: App name synchronizes through the shared identity override, and the organization display alias changes the shell heading only. The canonical credential remains visually and semantically unchanged. Workplace preview persistence advances to V2 with V1 interaction migration. Focused integration passes 8 files / 112 tests and dedicated Playwright passes 10/10 across desktop Chromium and simulated Pixel 5; day/night and zh/en variants preserve readable typography, touch targets, focus, long-text wrapping, persistence/reopen, and zero page-level horizontal overflow. Evidence remains under `output/e2e/workplace-app-shell/`; no physical-device evidence is claimed. This is still an S1 local preview, so visual completion does not imply organization ownership, attendance truth, credential grants, Calendar writes, fandom publishing rights, provider use, or Event Runtime integration.
125. `CLOUD PASTEL ANIMAL ICON PACK 12/20 INTEGRATED (2026-08-24)`: the second user-approved `ip-as-logo` asset round adds Network Wi-Fi clownfish V4, Wallet pouch kangaroo, Gallery photo-wing butterfly, Music note-spout whale, Appearance crested peacock V6, and Widgets tile-shell snail to the existing Phone, Camera, Weather, Calendar, Map, and Settings masters. Byte-identical accepted copies and SHA-256 evidence are frozen under `output/imagegen/cloud-pastel-animal-icons-accepted-round2/`, while stable runtime WebPs live in the existing `cloud-pastel-animals-v1` pack directory. The shared resolver now provides animal artwork for 12 of 20 allowlisted native-system Apps; Reminders, Contacts, Book, and App Store remain the next visible desktop candidates, while Files, Assets, World Hub, and Stock deliberately retain classic fallback because the user does not currently need dedicated desktop artwork for them. Day/night continues to use the shared night environment-light layer rather than duplicate raster assets. Explicit per-App overrides still win, and Chat plus commercial/branded Apps remain outside the pack. Roadmap priority is unchanged.
126. `SHP-8 THROUGH SHP-11 FOLLOW-ON SHELLS S1 DONE (2026-08-24)`: `联程 / VIA`, `谱权 / CREDO`, `递送 / POSTA`, and `机会 / NEXT` complete the accepted initial shell portfolio with category-specific structures for intercity timetable comparison, creator works/rights administration, parcel tracking and delivery inbox, and jobs/auditions/invitations. Each shell has an ordinary local loop, explicit unavailable/stale/fail-closed states, durable device-local preview state excluded from backup, Home/App Store/identity/return integration, and desktop plus simulated Pixel 5 day/night zh/en accessibility coverage with zero horizontal overflow. The four shells share no fake generic dashboard: their navigation, density, palette, card grammar, and primary action hierarchy follow their product category. They remain S1 previews and grant no booking, ticket, copyright, certification, shipment, application, payment, appointment, Map Journey, notification, or Event Runtime success. Dedicated portfolio Playwright passes 8/8; no physical-device evidence is claimed.
127. `VISUAL REFERENCE AND ART-DIRECTION BASELINE UPDATED (2026-08-26)`: every future beautification, redesign, brand, motion, spatial, 3D, or immersive visual-authoring task now begins with a compact three-to-five-example reference kickoff and one bounded project-local `ui-ux-pro-max` query. The new project-owned `visual-art-direction` skill converts those inputs into SchatPhone-specific brand grammar, motion language, spatial/3D reasoning, asset planning, and specialist routing; implementation then adds at most one focused specialist family. Existing `docs/design/*` contracts remain authoritative, `ui-ux-pro-max --persist` remains prohibited, public references authorize principle extraction rather than copying, and another PC receives the same reviewed skills through Git without an independent install. This changes visual workflow quality and cross-PC consistency only; roadmap priority and product scope do not change.
128. `LIQUID PRISM V1 + OPTIONAL COMPANION WIDGETS DONE (2026-08-26)`: Appearance now ships `水光玻璃 / Liquid Prism` as a neutral, nearly colorless glass system theme plus a 20-target native-system App material icon pack. The final user-directed art correction removes mint/aqua/blue as the theme identity: day mode uses pearl-gray ambient light, night mode uses graphite and silver, and only faint warm-silver/gray-violet edge reflections remain to describe refraction. Explicit App Store per-App icon overrides still win; Chat, commercial/branded Apps, existing widgets, Home templates, slots, and placements remain unchanged. The style kit may explicitly install five independent companion widgets (`Status`, `Capsule`, `Day`, `Player`, and `Agenda`) into Widget Center only; installation is idempotent, never auto-places them on Home, and switching themes never removes them, so users can freely mix default, Cloud Pastel, Liquid Prism, legacy widgets, and these new glass widgets. The current recommended day/night wallpaper is a complete CSS-rendered neutral light field rather than a committed raster asset. Validation passes lint, the 691-module production build, governance 14/14, five focused Vitest files 74/74 including the isolated persistence inventory, the dedicated desktop/simulated-mobile Appearance flow 10/10, the visual gate 20/20, and live day/night browser inspection with no page errors. The broader `npx vitest run tests` reaches 326 passing project files and 2435/2436 passing project tests but exits non-zero because it also scans four ignored local `.codex` Jest suites and the persistence inventory times out once under full parallel load; that inventory passes in the focused 74/74 rerun. Roadmap priority is unchanged.
129. `LIQUID PRISM LINE-GLYPH PILOT + REVERSIBLE PACK PRIORITY LOCAL REVIEW (2026-08-26)`: the first eight user-supplied outline SVGs now render inside the existing nearly transparent Liquid Prism shell for Network, Wallet, Camera, Map, Calendar, Reminders, Widgets, and Appearance. Appearance adds a persisted `优先显示套装图标 / Prefer pack icons` switch so users with historical App Store icon overrides can temporarily see the selected system pack across Home, Dock, App Store, lock/banner, and Notification Center surfaces without deleting the saved icon or display name; disabling it restores the prior visual immediately. Chat and commercial/branded Apps remain outside the allowlist. Focused Vitest and the dedicated reversible-priority Chromium journey pass, and live phone-sized inspection confirms zero horizontal overflow and the expected 3px restrained backdrop blur. The 13-source Iconfont collection remains a local visual candidate until its publication/license boundary is explicitly confirmed; this pilot does not claim those vectors as release-cleared artwork. Roadmap priority is unchanged.
130. `CHROMATIC GLASS EXPERIMENTAL THEME (2026-08-26)`: Appearance now exposes `彩光玻璃 / Chromatic Glass` as an independent interface theme and one-click style kit. It keeps the existing Liquid Prism line-glyph pack, but adds a pastel ambient field and low-saturation pink, powder blue, mint, and peach in edge light, semantic accents, and restrained shadows; the glass body remains translucent so the direction can be compared against neutral Liquid Prism without changing icon ownership. The kit does not install or place widgets, does not touch personal wallpapers unless the user explicitly enables the kit wallpaper option, and preserves per-App overrides, Chat, commercial logos, Home layout, and existing widgets. Focused style-foundation Vitest passes 22/22, the Chromatic Glass Playwright journey passes 2/2 across desktop and simulated mobile, lint and the 692-module production build pass, and phone-sized inspection reports zero horizontal overflow in day and night. This remains a first visual experiment pending user review; no new raster assets or Iconfont publication claim is introduced.
131. `CHROMATIC GLASS REFERENCE CORRECTION (2026-08-26)`: after review against the user's pastel liquid-glass component reference, Chromatic Glass now keeps the light field near neutral gray and moves the visible color into transparent control surfaces, per-action blue/mint/peach/pink layers, inner highlights, edge refraction, and tinted soft shadows. Liquid Prism remains the nearly colorless alternative; the Chromatic kit still reuses the existing line-glyph pack and does not install or place widgets. Focused appearance Vitest passes 69/69 across four files, the Chromatic Glass Playwright journey passes 2/2 across desktop and simulated mobile, the visual gate passes 20/20, lint and the 692-module production build pass, and governance passes 14/14. No raster asset or Iconfont publication claim is introduced; roadmap priority is unchanged.
132. `CHROMATIC GLASS CONSOLIDATION (2026-08-26)`: the user-selected Chromatic Glass direction now replaces Liquid Prism as the single public glass theme, style kit, and system App icon-pack name. The combined kit keeps Chromatic Glass's current pastel edge-light interface and line-icon treatment while inheriting Liquid Prism's five optional companion widgets; widget installation remains explicit, idempotent, library-only, and never changes Home placement. Persisted or imported `liquid-prism` system-theme, style-kit, and icon-pack IDs normalize to `chromatic-glass`, so existing users keep a complete applied kit instead of falling back or becoming customized. The legacy widget collection and icon material identifiers remain internal compatibility keys only and are no longer exposed as a second theme. Chat, commercial App identities, personal wallpaper policy, per-App overrides, existing widgets, and Home layout remain unchanged. Focused appearance tests pass 56/56, the combined affected and persistence suite passes 104/104, the targeted Appearance Playwright journey passes 14/14, the visual gate passes 20/20, governance passes 14/14, lint passes, the 693-module production build succeeds, and `git diff --check` is clean. The full Vitest run passes 2449/2450 tests across 327/328 files; its sole failure is a 5-second timeout in `persistence-owner-inventory.test.js` under full parallel load, while the same file passes 9/9 in isolation. Roadmap priority is unchanged.
133. `CHROMATIC GLASS MATERIAL REFINEMENT (2026-08-26)`: Chromatic Glass now follows the accepted clear-body, colored-edge reference more closely without copying its component composition. System App glyphs render as crisp dark single-color paths instead of gray fills surrounded by white strokes; blue, pink, mint, peach, and lilac move into the thicker refractive rim, lower inner light, and softly tinted shadow. Home and Dock icon shells, Appearance cards, kit actions, toggles, and checkboxes now share the same near-colorless layered-glass material, while the night variant reduces silver wash and retains legible pale glyphs on dark glass. App meaning, theme behavior, persistence, independent Chat ownership, commercial identities, personal wallpapers, per-App overrides, and Home layout remain unchanged. Focused Vitest passes 23/23, the Chromatic Glass Playwright journey passes 2/2 across desktop and simulated mobile with an explicit no-stroke glyph regression check, the visual gate passes 20/20, lint and the 693-module production build pass, and `git diff --check` is clean. The full Vitest run passes 2450/2451 tests across 327/328 files; its sole failure is the existing 5-second full-parallel timeout in `persistence-layer-reconcile.test.js`, while that file passes 27/27 in isolation. Roadmap priority is unchanged.
134. `MOONLIT JOURNAL PAPER-WHITE CORRECTION (2026-08-26)`: Appearance keeps `月光手账 / Moonlit Journal`, but its visual thesis is corrected from champagne-and-coffee vintage paper to `月光纸白 / Moonlit Paper White` after comparison with the user's reference. Day mode now uses high-lightness warm white, misty blush, and pale taupe; night mode remains a smoky pink-gray paper surface only one brightness step below day, with dark text instead of deep coffee backgrounds. The one-click kit still composes the theme with the classic system icon pack and introduces no raster wallpaper, companion widget, layout mutation, Chat styling, commercial-logo replacement, or per-App override change.
135. `STICKER POP THEME + SYSTEM ICON MATERIAL (2026-08-26)`: Appearance now exposes `贴纸涂鸦 / Sticker Pop` as a complete system theme and one-click kit based on the user's two bold-outline references and the existing style taxonomy. Day uses creamy paper, dark rounded outlines, short hard shadows, and softened coral/yellow/green/sky/lilac blocks; night uses warm graphite, reduced-saturation blocks, and pale cream outlines so the sticker identity survives without harsh color collision. Its `贴纸粗线 / Sticker Outline` system App pack covers the existing native-system allowlist through recognizable Font Awesome glyphs in a shared bold-outline material shell; Chat, commercial/branded Apps, explicit per-App overrides, Home layout, widgets, and app-owned skins remain unchanged. The pack deliberately uses no external reference artwork or raster assets. The combined Moonlit Journal correction and Sticker Pop slice passes lint, the full 328-file / 2458-test Vitest suite, the 693-module production build, four focused desktop/simulated-mobile Playwright cases, the 20-case visual gate, governance 19/19, `git diff --check`, and live phone-sized day/night Home inspection. Roadmap priority is unchanged.
136. `ONDAM CARE ELEPHANT LOGO V2 + IP ASSET INVENTORY (2026-08-27)`: Healthcare now uses the user-approved V2 baby-elephant doctor across the shared Home/App Store identity resolver and the initial Healthcare mascot slots. V2 preserves the accepted warm ivory and powder-blue palette, compact proportions, and calm facial expression while increasing trunk/face separation and adding a rounded warm-ivory medical `+` to the slate bib. The verified 1254-square PNG is registered as `shared/app-icons/ondam-care-app-icon-v2.png`; V1 remains historical evidence. `HEALTHCARE_IP_ASSET_PLAN.md` maps a maximal optional family of 56 deliverables: 8 brand masters, 6 expressions, 10 half/full-body actions, 10 props, 12 product-state illustrations, and 10 small or alternate treatments, with export rules and medical-boundary exclusions. Asset registry validation passes with 1115 registered assets and zero violations; focused Vitest passes 25/25, lint and the 695-module production build pass, governance passes 19/19, Healthcare route Playwright passes 8/8, and shared brand-icon loading passes 4/4 across desktop Chromium and simulated mobile after real remote-asset prewarming. Live browser inspection confirms the 1254-square V2 loads at 44-square header, 88-square hero, Home, and App Store sizes without changing the Healthcare product boundary. Roadmap priority is unchanged.
137. `CREAM SHELL THEME + BUILT-IN WALLPAPER + OPTIONAL COMPANION WIDGETS DONE (2026-08-27)`: Appearance now ships `奶油软壳 / Cream Shell` as an independent system theme and one-click style kit rather than folding the direction into Moonlit Journal. Its day anchor is `#FCEAD9` cream-peach, while Dock, cards, controls, icon tiles, and five companion widgets use restrained ivory and vanilla blocks instead of yellow caramel, flat white, or pink object surfaces. Night lowers the same cream family one brightness step instead of becoming a dark coffee theme; neutral dark ink maintains readable status text, App labels, and page indicators. The kit composes the theme with the dedicated 20-target `奶油细线 / Cream Line` native-system App pack and may explicitly install five independent companion widgets (`Status`, `Focus`, `Day`, `Player`, and `Agenda`) into Widget Center only. It offers the original built-in `shared/backgrounds/cream-shell-soft-haze-v2.webp`, which supplies pale blush and cream environmental color without copying referenced characters, brand text, watermarks, simulated cards, or interface containers. Applying the wallpaper remains optional, personal wallpaper remains a supported combination, widget installation is idempotent, widgets are never auto-placed on Home, and switching themes never removes them. Home layout, existing widgets, Chat, commercial identities, and per-App overrides remain independent, and Cream Shell stays visually and semantically separate from Moonlit Journal's paper-white serif journal language. The image-bed destination verifies 2/2 with a versioned runtime URL to prevent stale browser imagery; the asset registry reports zero violations. Focused Vitest passes 76/76, the dedicated Cream Shell Playwright journey passes 2/2 across desktop and simulated mobile, the visual gate passes 20/20, scoped lint passes, the 699-module production build passes, governance passes 19/19, and `git diff --check` is clean. Live phone-sized Home inspection confirms both day and night modes use the new wallpaper, `#FCEAD9` day tokens, ivory icons and Dock, readable dark glyphs, and zero horizontal overflow. Roadmap priority is unchanged.
138. `CREAM SHELL REFERENCE INTERPRETATION + ZERO-DEPTH CORRECTION (2026-08-27)`: User review first rejected a generated wallpaper whose oversized rounded panels and broad taupe divider competed with real Home content. The replacement `shared/backgrounds/cream-shell-soft-haze-v2.webp` therefore removes all simulated cards, hard partitions, characters, copy, and foreground decoration, retaining only low-contrast cream-peach, blush, vanilla, a quiet central light field, subtle blurred ribbons, and restrained grain. Review of the complete reference image then established the more important correction: its strongest soft-clay depth comes from the physical phone case, device edges, studio lighting, and whole-device shadow, not from the Home desktop. Cream Shell is therefore classified as a flat cream-block theme rather than a soft-clay or embossed theme. Blush remains environmental wallpaper color; icon tiles, Dock, system cards, companion widgets, and the Lock notification stack use near-flat vanilla-ivory surfaces with no shaping shadow, relying on small color differences and restrained borders for separation. The Lock notification center now replaces its previous hard-coded white-on-light treatment with dark system text, cream notification cards and actions, and a subdued narrow scrollbar. The wallpaper stays optional and does not alter widget, layout, Chat, commercial identity, or per-App ownership. The runtime WebP and protected SVG source verify 2/2 through the project image bed; the asset registry reports 1122 tracked assets and zero violations after removing both rejected V1 registry entries. Focused Vitest passes 76/76, the Cream Shell Playwright journey passes 2/2 across desktop and simulated mobile including the Lock notification surface, the visual gate passes 20/20, lint passes, the 700-module production build passes, governance passes 19/19, `git diff --check` is clean, and live phone-sized inspection confirms readable dark notification copy, flat cream cards, no horizontal overflow, and no console errors. The full test command remains non-green because the local sandbox denies `lstat` on the short user-profile path `C:\Users\ADMINI~1` inside four image-bed fixture tests; those tooling tests pass 11/15 in isolation and are outside this visual slice. Roadmap priority is unchanged.
139. `ONDAM CARE CORE IP + FIRST SCENE INTEGRATION (2026-08-27)`: the user-approved Healthcare IP set now has a completed 20-item core rather than an implied requirement to produce the full optional family. Five lightweight runtime WebPs integrate the accepted art into the existing shell: a full welcome hero, restrained appointment and report page leads, a privacy-sheet scene, and a magnifier pose for search no-result recovery. The implementation preserves Healthcare navigation, appointments, reports, fail-closed source handling, current palette, day/night behavior, and the read-only Map handoff. Healthcare institutions continue to hold versioned `Map` place references (`owner: map`, `recordType: place`, record id, revision, and world id); the map pin is only Map's visual presentation, not the imported source object. The five verified runtime assets total about 109 KB. Focused Vitest passes 9/9, lint passes, the 699-module production build passes, the Healthcare Playwright route passes 8/8 across desktop Chromium and mobile Chrome, the asset registry reports 1124 tracked assets with zero violations, and live 340 px plus 1280 px inspection confirms no horizontal overflow or console errors. Roadmap priority is unchanged; the remaining 36-item expansion pool stays optional and screen-driven.
140. `ONDAM CARE CONSUMER COMPANION STRUCTURE CORRECTION (2026-08-27)`: user review correctly identified that the first scene integration still sat inside a generic workbench scaffold. The Healthcare root surfaces now replace the double-column institution card wall, paired dashboard cards, pill-filter row, boxed appointment cards, boxed report rows, and detached page thumbnails with a calmer consumer-care composition: an immersive welcome scene, underline category navigation, one continuous personal-care gateway, a single-column care directory, timeline-like appointments, folio-like report rows, and scene art integrated into the appointment and report page fields. The header also loses its toolbar divider and heavy control shells. Navigation, local booking, report revision handling, withdrawn-source fail-closed behavior, Map place references, day/night themes, and all test ids remain unchanged. Focused Vitest passes 9/9, lint passes, the 699-module production build passes, Healthcare Playwright passes 8/8 across desktop Chromium and mobile Chrome, and live 396 px plus 1280 px inspection confirms zero horizontal overflow. Roadmap priority is unchanged.
