# AI 需求话术模板（给 AI 编程助手）

用途：你在其他 AI 工具里提需求时，按这个模板发，结果会稳定很多。

---

## 1. 任务目标

请在现有 SchatPhone 项目中完成：
- [这里写 1 句话目标]

## 2. 必做与禁做

- 必做：
1. [必做 1]
2. [必做 2]

- 禁做：
1. 不要改路由结构（除非我明确要求）
2. 不要删除现有可用功能
3. 不要在组件里直写 AI fetch

## 3. 项目约束（固定）

- 技术栈：Vue 3 + Vite + Vue Router + Pinia + Tailwind v4
- AI 调用统一走：`src/lib/ai.js`
- Settings 与 Home 不做重复入口堆叠
- 所有用户输入页需有显式“保存按钮”
- 主题优先用 CSS 变量，不要硬编码大量颜色

## 4. 变更范围

请仅修改以下文件（可增删）：
- [文件 1]
- [文件 2]

请不要修改以下文件：
- [文件 A]
- [文件 B]

## 5. 交互要求

- 目标页面：[/settings /home /network ...]
- 关键交互：[点击、滑动、下拉、保存反馈等]
- 视觉风格：[Y2K / 纯白 / iOS setting 风格]

## 6. 验收标准

完成后请确保：
1. `npm run lint` 通过
2. `npm run build` 通过
3. 我指定的页面行为可用

## 7. 交付格式

请直接改代码，并在回复里给我：
1. 修改了哪些文件
2. 每个文件改了什么
3. 还剩什么风险或下一步建议

---

## 可直接复制版本

请在 SchatPhone 项目中完成以下任务：
1. 目标：[填写]
2. 必做：[填写]
3. 禁做：不要破坏现有路由与可用模块
4. 技术约束：Vue3 + Vite + Pinia + Tailwind v4，AI 统一走 `src/lib/ai.js`
5. 结构约束：Settings 与 Home 不重复堆入口；输入页必须有“保存按钮”
6. 修改范围：[填写文件]
7. 验收：跑 `npm run lint`、`npm run build` 并说明结果
8. 输出：列出改动文件与要点
