# AI 协作话术模板（给 AI 编程助手）

用途：在其他 AI 工具中发需求时，提升可执行性和稳定性。

---

## 1. 任务目标

请在现有 SchatPhone 项目中完成：
- [一句话目标]

## 2. 必做与禁做

- 必做：
1. [必做 1]
2. [必做 2]

- 禁做：
1. 不要随意改路由结构（除非我明确要求）
2. 不要删除现有可用功能
3. 不要在组件里直写 AI fetch

## 3. 项目约束（固定）

- 技术栈：Vue 3 + Vite + Vue Router + Pinia + Tailwind v4
- AI 调用统一走：`src/lib/ai.js`
- Home 与 Settings 避免重复入口
- Home 规则：`app_*` 入口不可删除/隐藏，只能换位置
- 输入页需要显式“保存按钮”并给保存反馈
- Network / Chat 相关改动需要考虑错误分级提示与重试交互
- 主题优先走 CSS 变量，避免大量硬编码

## 4. 变更范围

请仅修改以下文件：
- [文件 1]
- [文件 2]

请不要修改以下文件：
- [文件 A]
- [文件 B]

## 5. 交互要求

- 目标页面：[/home /settings /network /appearance ...]
- 关键交互：[点击、滑动、长按、拖拽、保存反馈]
- 若涉及网络调用：[错误提示分级 + 重试按钮行为]
- 视觉风格：[Y2K / 纯白 / iOS setting 风格]

## 6. 验收标准

完成后请确保：
1. `npm run lint` 通过
2. `npm run build` 通过
3. 涉及行为逻辑时 `npm run test` 通过
4. 指定页面行为可复现

## 7. 交付格式

请直接改代码，并在回复里给我：
1. 修改了哪些文件
2. 每个文件改了什么
3. 还剩什么风险或下一步建议

---

## 可直接复制版本

请在 SchatPhone 项目中完成以下任务：
1. 目标：[填写]
2. 必做：[填写]
3. 禁做：不要破坏现有路由、Home 锁定入口规则和可用模块
4. 技术约束：Vue3 + Vite + Pinia + Tailwind v4，AI 统一走 `src/lib/ai.js`
5. 结构约束：Home 与 Settings 不重复堆入口；输入页必须有“保存按钮”
6. 修改范围：[填写文件]
7. 验收：运行 `npm run lint`、`npm run build`，必要时 `npm run test`
8. 输出：列出改动文件、关键变更和风险
